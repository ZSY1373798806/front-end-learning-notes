## 🚀 打包部署

- **常见方案**：Webpack、Vite、Rollup、Rspack、Rsbuild
- **优化目标**
  - 产物瘦身（Tree-shaking、Code splitting、压缩混淆）
  - 静态资源优化（图片压缩、懒加载、CDN）
  - 缓存利用（hash、HTTP 缓存、Service Worker）

### ESLint & Prettier & Husky & lint-staged

| 工具            | 核心目标                      | 解决的核心问题                                               | 作用层面               | 关键特点                                                     |
| --------------- | ----------------------------- | ------------------------------------------------------------ | ---------------------- | ------------------------------------------------------------ |
| **ESLint**      | 代码质量检查与错误预防        | 避免潜在 Bug（如未定义变量、死循环）、强制编码规范（如禁用 `var`） | 代码逻辑 / 语法层面    | 可检查「质量问题」（如 `no-unused-vars`），也可管部分「格式问题」（如缩进），但非专注格式 |
| **Prettier**    | 代码格式统一（视觉风格）      | 解决团队格式混乱（如有人用单引号、有人用双引号；换行长度不一致） | 代码视觉格式层面       | 只管「格式」，不管「质量」，100% 可自动修复，规则极简（如 `printWidth`、`semi`） |
| **Husky**       | 管理 Git 钩子，触发自定义脚本 | 确保 Git 操作（如 `commit`、`push`）前必须执行校验（避免跳过检查） | Git 工作流层面         | 相当于「触发器」，不直接做校验，只负责在指定 Git 事件时调用其他工具（如 ESLint、lint-staged） |
| **lint-staged** | 只对 Git 暂存区文件执行校验   | 避免每次校验所有文件（效率低），只检查即将提交的代码（精准性） | Git 暂存区文件筛选层面 | 依赖 Husky 的钩子触发，不直接做校验，只负责「筛选文件」，再调用 ESLint/Prettier 处理筛选后的文件 |

- **ESLint、Prettier**

  聚焦「代码本身」（质量 / 格式）

- **Husky、lint-staged**

  聚焦「Git 工作流」（提交校验的时机 / 范围）

- **典型工作流：看四者如何协同（更易理解区别）**

  以「用户提交代码」为例，四者的协同流程能清晰体现各自的角色：

  - 1.用户修改代码后，执行 `git add xxx.js`（将文件加入暂存区）；
  - 2.用户执行 `git commit -m "修改xxx"`；
  - 3.**Husky** 检测到 `commit` 操作，触发预设的 `pre-commit` 钩子；
  - 4.**lint-staged** 接收到 Husky 的触发信号，读取暂存区文件列表（只取 `git add` 的文件）；
  - 5.**Prettier** 先对这些文件进行格式化（统一引号、换行等）；
  - 6.**ESLint** 再对格式化后的文件做质量检查（如检查未使用变量、禁用 `eval`）；
  - 7.若 Prettier 格式化成功且 ESLint 无错误 → 允许 `commit`；若有错误（如 ESLint 发现未使用变量）→ 阻止 `commit`，提示用户修复。

- **总结：核心区别一句话概括**

  - **ESLint**：管「代码对不对」（质量）；

  - **Prettier**：管「代码好不好看」（格式）；

  - **Husky**：管「什么时候检查」（Git 钩子触发）；

  - **lint-staged**：管「检查哪些文件」（只查暂存区）。

四者不是替代关系，而是「代码校验（ESLint+Prettier）」与「流程保障（Husky+lint-staged）」的组合，共同确保代码质量和团队协作效率。

<img src="https://i-coder.oss-cn-beijing.aliyuncs.com/files/20250902092618.png" style="zoom: 36%;" />

### 配置Husky & lint-staged

- 安装依赖

  ```bash
  npm install commitizen cz-conventional-changelog @commitlint/cli @commitlint/config-conventional husky --save-dev
  ```

- 配置Commitizen

  ```json
  {
    "config": {
      "commitizen": {
        "path": "./node_modules/cz-conventional-changelog"
      }
    },
    "scripts": {
      "commit": "git-cz"
    }
  }
  ```

- 配置Husky钩子

  ```bash
  npx husky install
  npm pkg set scripts.prepare="husky install"
  ```

- 创建禁止直接 commit 的钩子 (核心)

  ```bash
  npx husky add .husky/pre-commit
  ```

  编辑.husky/pre-commit内容

  ```bash
  #!/bin/sh
  
  # 检查是否通过 commitizen 提交
  if [ -z "$CZ_TYPE" ]; then
    echo "Error: Direct 'git commit' is disabled. Use 'npm run commit' instead." >&2
    exit 1
  fi
  ```

- 配置 Commitizen 传递环境变量

  修改 `package.json` 中的 commit 脚本

  ```json
  {
    "scripts": {
      "commit": "CZ_TYPE=true git-cz"
    }
  }
  ```

- 配置 commitlint (可选但推荐)

  创建 `.commitlintrc.json`

  ```json
  {
    "extends": ["@commitlint/config-conventional"]
  }
  ```

  创建 `.husky/commit-msg`

  ```bash
  npx husky add .husky/commit-msg 'npx --no commitlint --edit "$1"'
  ```

### Axios

- #### 取消请求

  - CancelToken

    ```js
    import axios from 'axios';
    
    // 创建取消令牌源
    const CancelToken = axios.CancelToken;
    const source = CancelToken.source();
    
    // 发送请求
    axios.get('/api/data', {
      cancelToken: source.token
    }).catch(err => {
      if (axios.isCancel(err)) {
        console.log('请求取消:', err.message);
      } else {
        // 处理其他错误
      }
    });
    
    // 取消请求
    source.cancel('用户主动取消请求');
    ```

  - AbortController 

    ```js
    const controller = new AbortController();
    
    axios.get('/api/data', {
      signal: controller.signal
    }).catch(err => {
      if (err.name === 'AbortError') {
        console.log('请求取消');
      }
    });
    
    // 取消请求
    controller.abort();
    ```

    

## 🏗️ 最佳实践组合

1. **开发中**
   - ESLint + Prettier 实时检测（编辑器插件 + 保存自动修复）
   - Axios + AbortController 管理请求
2. **提交前**
   - Husky (pre-commit) + lint-staged → 只检查本次修改文件
   - Commitizen 统一交互式提交
   - Commitlint 校验提交规范
3. **打包部署**
   - Vite/webpack 打包优化
   - CI/CD 自动执行 Lint + Test
   - 部署前构建产物压缩、分发 CDN