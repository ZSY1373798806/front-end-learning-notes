## **1️⃣ 浏览器中的事件循环（Event Loop）**

### **核心概念**

事件循环是 **js执行机制的核心**，它负责协调 **执行栈（Call Stack）**、**任务队列（Task Queue）** 和 **浏览器渲染**，保证 **异步任务** 能够有序执行

事件循环是js运行时环境中的一个核心概念，负责协调异步操作和同步代码的执行

- JavaScript 是单线程的，所有同步任务在 **执行栈（Call Stack）** 执行。
- 异步任务不会阻塞主线程，而是交给 **浏览器环境（Web APIs）** 处理。
- 异步任务分为：
  - **宏任务（Macrotask）**：`setTimeout`、`setInterval`、`setImmediate`（Node）、`I/O`、`requestAnimationFrame`、requestIdleCallback
  - **微任务（Microtask）**：`Promise.then`、`MutationObserver`、`queueMicrotask`

> 🔑 特点：**微任务优先于宏任务执行**，在一次事件循环（tick）中，先清空微任务，再执行宏任务。

------

### **任务队列与执行顺序**

1. **执行栈**：同步代码依次入栈执行。

2. **异步任务**：遇到异步函数，将回调交给 **Web APIs**，等待事件完成。

3. **微任务队列**：回调放入微任务队列，如 `Promise.then`。

4. **宏任务队列**：回调放入宏任务队列，如 `setTimeout`。

5. **事件循环流程**：

   ```sql
   循环步骤：
   1️⃣ 执行栈中的同步代码
   2️⃣ 同步代码执行完，清空微任务队列
   2.5 这个可能会让出线程，浏览器内部操作（渲染 / GC / I/O 处理）（不一定每次都有）
   3️⃣ 执行宏任务队列中的第一个任务
   4️⃣ 宏任务执行完，再次清空微任务队列
   5️⃣ 重复宏任务 -> 微任务 -> 宏任务循环
   ```

   ```sql
   ┌──────────────────────────────┐
   │        调用栈（Call Stack）     │
   │  执行当前同步任务，例如脚本和函数调用 │
   └───────────────┬───────────────┘
                   │ 同步执行完毕
                   ▼
         +-------------------+
         | 首次「微任务清空」  |
         | （执行 Promise.then、MutationObserver 等） |
         +-------------------+
                   │ 完全清空后
                   ▼
         ┌────────────────────────┐
         │ 浏览器可能“让出线程”阶段 │
         │ （渲染 Repaint / 排版 Reflow / GC / I/O 回调等） │
         └────────────────────────┘
                   │ 准备进入下一轮循环
                   ▼
         +-------------------+
         | 下一个宏任务（Macrotask，如 setTimeout） |
         +-------------------+
                   │ 执行中可生成同步任务或微任务
                   ▼
         +-------------------+
         | 再次清空微任务队列   |
         +-------------------+
                   │ 然后可能再次“让出线程”
                   ▼
         ... 重复上述流程 ...
   ```

6. **能否让出线程**：

   - **微任务之间不能让出线程** → 保证当前宏任务相关状态完全同步，避免 UI 或数据中间态。
   - **宏任务之间可以让出线程** → 每个宏任务都是独立单元，执行完微任务后，浏览器渲染不会造成不一致，允许安全渲染、GC 或处理 I/O。
   - 核心差异在于 **“是否依赖当前宏任务状态”**：微任务依赖，宏任务独立。

> ✅ 理解关键点：
>
> - 同步代码最先执行
> - 微任务总在当前宏任务结束后立即执行
> - 每个宏任务开始前，微任务队列一定是空的

![](https://i-coder.oss-cn-beijing.aliyuncs.com/files/20220208235541.png)

<img src="https://i-coder.oss-cn-beijing.aliyuncs.com/files/20250905215150.png" style="zoom: 25%;" />

### **MutationObserver 示例**

```js
const targetNode = document.getElementById('some-id');
const config = { attributes: true, childList: true, subtree: true };
const observer = new MutationObserver((mutationsList) => {
  console.log('DOM变化了', mutationsList);
});
observer.observe(targetNode, config);

// 停止观察
// observer.disconnect();
```

- 它属于微任务，优先级高于宏任务。

### requestIdleCallback

`requestIdleCallback` 是浏览器提供的一个**宏任务调度 API**，但它有些特别，不同于普通的 `setTimeout` 或 `setInterval`。我们仔细分析一下：

------

## **1️⃣ 基本概念**

- 用途：在浏览器**空闲时间**执行一些低优先级任务（例如，日志收集、预加载、非紧急计算）。
- 语法：

```js
requestIdleCallback(callback, options);
```

- **参数**：
  - `callback(deadline)`：当浏览器空闲时执行的函数，`deadline` 对象提供剩余空闲时间。
    - `deadline.timeRemaining()`：返回当前空闲时间（ms），可用于决定是否继续执行耗时操作。
    - `deadline.didTimeout`：表示是否因为超时而被执行。
  - `options`：
    - `timeout`：可选，强制在指定时间后执行回调，即使浏览器仍然忙碌。

------

## **2️⃣ 执行机制**

- **类型**：宏任务（Macrotask）
- 但是有特点：
  - 它不会立即执行，而是**等待浏览器空闲**。
  - 当浏览器有空闲帧时才执行。
  - 优先级低于普通宏任务（如 `setTimeout`），不会阻塞页面渲染。

> 🔹 类比：
>
> - `setTimeout(fn, 0)` → 尽快执行
> - `requestIdleCallback(fn)` → 在浏览器空闲时执行

------

## **3️⃣ 示例**

```js
requestIdleCallback((deadline) => {
  while (deadline.timeRemaining() > 0 && tasks.length > 0) {
    const task = tasks.shift();
    task();
  }
}, { timeout: 2000 }); // 最多等2秒
```

- 当浏览器空闲时，会批量处理任务。
- 如果空闲时间不足，可以等下一次空闲再继续。

------

## **4️⃣ 注意事项**

- 不是每个浏览器都支持（IE 不支持）。
- 不保证立即执行，所以不能用来做关键业务逻辑。
- 更适合执行非关键、可延迟的操作。



## 🔎 问题

### 1️⃣ 清空微任务队列过程中，插入同步任务？

- **不会插入同步任务**。
   因为同步任务只有在调用栈最顶层时运行，已经跑完了主线程的同步阶段。
- 在清空微任务的过程中，只能再加入新的 **微任务**。
   → 如果在执行 `Promise.then` 时，再创建一个新的 `Promise.then`，它会**继续加入到当前正在清空的微任务队列末尾**。

------

### 2️⃣ 清空宏任务队列过程中，插入同步任务或微任务？

- “清空宏任务队列” 这个说法不太准确，**宏任务是一次只执行一个**。
- 在执行某个宏任务时，可以插入 **同步代码**（立刻执行）和 **新的微任务**。
- 当前宏任务执行完后，会立刻清空刚刚插入的微任务队列。

------

### 3️⃣ 清空微任务队列 [a, b, c] 过程中，能中断吗？

- **不能被中断**。
- 规则：**必须一次性清空所有微任务**，直到队列为空才会进入下一个宏任务。
- 但是！在清空过程中，执行的某个微任务可以再加入新的微任务，这些会追加到队列后面。
   → 所以清空过程可能变成 [a, b, c, d, e, ...]，但依旧是 “一次性清空到底”。

------

### 4️⃣ 清空宏任务队列 [e, f, g] 过程中，能中断吗？

- 注意：宏任务不是 “清空”，而是 **一个一个取出来执行**。
- 执行完当前宏任务（比如 e）后，必须先清空所有微任务，再执行下一个宏任务（f）。
- 所以 **在一个宏任务执行过程中是不会中断的**，但执行完一个宏任务，清空微任务以后，事件循环有机会去暂停（比如 UI 渲染、I/O 回调）。

------

## 🎯 总结（简表）

| 阶段                      | 能插入新任务？                            | 能中断吗？                                                   |
| ------------------------- | ----------------------------------------- | ------------------------------------------------------------ |
| 清空微任务队列            | ✅ 可以插入新的微任务（追加到队尾）        | ❌ 不能中断，必须一次性清空                                   |
| 执行宏任务                | ✅ 可以执行同步代码、插入新的微任务/宏任务 | ❌ 当前宏任务执行过程中不能中断                               |
| 执行完宏任务 → 清空微任务 | ✅ 会先处理微任务                          | 宏任务执行后，清空微任务后， ⏸ 可以在这时“暂停”，交给渲染/UI 刷新等 |



------



## **Node.js 中的事件循环**

#### 一、核心原理

- Node 使用 **V8 引擎** 执行 JS。
- I/O 采用 **libuv**，是跨平台的事件驱动库。
- Node 的事件循环阶段比浏览器更复杂，分为多个阶段，每个阶段处理特定任务：

#### 二、事件循环的阶段划分（6 个核心阶段）

​	Node.js 的事件循环按固定顺序循环执行 6 个阶段，每个阶段对应一个 “回调队列”，仅当当前阶段的队列清空后，才会进入下一个阶段。

| 阶段                                | 作用                                                         |
| ----------------------------------- | ------------------------------------------------------------ |
| **timers（定时器阶段）**            | 执行 `setTimeout()`、`setInterval()` 注册的回调（延迟时间 ≥ 1ms，实际可能因循环阻塞略长）。 |
| **pending callbacks（待处理回调）** | 执行上一轮循环中被延迟的 I/O 回调（如 TCP 连接错误回调、DNS 解析回调等，非用户代码直接注册） |
| idle, prepare                       | 仅 Node.js 内部使用（用户代码无法操作），用于空闲检测和准备工作 |
| **poll（轮询阶段）**                | 核心阶段，两大作用：<br/>① 执行当前就绪的 I/O 回调（如 `fs.read`、`http.request` 的回调）；<br/>② 若没有待执行的 I/O 回调，会阻塞等待，直到有新 I/O 事件或 `timers` 阶段的任务到期。 |
| **check（检查阶段）**               | 执行 `setImmediate()` 注册的回调（设计目的：I/O 回调后 “立即” 执行，专门用于 “在 poll 阶段后立即执行” 的场景） |
| **close callbacks（关闭回调）**     | 执行一些关闭回调，如 `socket.on('close', ...)`               |

<img src="https://p1-juejin.byteimg.com/tos-cn-i-k3u1fbpfcp/8271bd61ab934b5c90443c9d287ea139~tplv-k3u1fbpfcp-jj-mark:3024:0:0:0:q75.awebp#?w=1028&h=848&s=85985&e=jpg&b=110f12" alt="node_66.jpg" style="zoom: 50%;" />

#### 三、微任务与宏任务：事件循环中的优先级

​	事件循环的每个阶段执行完一个宏任务后，会**先清空所有微任务队列**，再进入下一个宏任务。Node.js 中微任务和宏任务的分类与优先级需特别注意：

##### 1. `process.nextTick()`

​	独立队列，优先级高于其他微任务；会在当前操作完成后立即执行，不受事件循环阶段影响，是在本轮循环执行的

##### 1. 微任务（Microtasks）：

​	`Promise.then/catch/finally`、`queueMicrotask()`

​	微任务（Micro-task）不归属事件循环的任何阶段，而是在**宏任务执行后、下一个宏任务 / 阶段切换前**执行

##### 2. 宏任务（Macrotasks）：低优先级，按阶段执行

​	宏任务即上述 6 个阶段中处理的回调，如 `setTimeout`、`setImmediate`、I/O 回调等，按阶段顺序执行。

#### 四、Node.js 12 事件循环 6 个阶段的**固定执行顺序**

6 个阶段按以下顺序**依次执行**，形成闭环循环：
`timers` → `pending callbacks` → `idle/prepare` → `poll` → `check` → `close callbacks` →（回到）`timers`

#### 五、详细执行流程（含阶段内逻辑与阶段切换条件）

每个阶段的核心逻辑、处理的任务类型，以及阶段间的切换规则如下：

#### 1. 阶段 1：`timers`（定时器阶段）

- **核心任务**：执行 `setTimeout()`、`setInterval()` 中**延迟时间已到期**的回调函数。
  - 延迟时间是 “≥1ms”（Node.js 会将 `0ms` 强制调整为 `1ms`）；
  - 仅执行 “当前时间 ≥ 定时器注册时的延迟时间” 的回调（未到期的会留在队列中，等待下一轮循环）。
- **阶段切换条件**：
  当该阶段所有 “已到期的定时器回调” 执行完毕后，自动进入下一阶段（`pending callbacks`）。

#### 2. 阶段 2：`pending callbacks`（待处理回调阶段）

- **核心任务**：执行**上一轮循环中被延迟的系统级 I/O 回调**（非用户主动注册的回调，如 TCP 连接错误、DNS 解析失败等底层回调）。
  - 这类回调数量极少，通常用户代码不会直接触发。
- **阶段切换条件**：
  该阶段的回调队列清空后，进入下一阶段（`idle/prepare`）。

#### 3. 阶段 3：`idle/prepare`（空闲 / 准备阶段）

- **核心任务**：仅 Node.js 内部使用（用户代码无法操作）。
  - `idle`：检测事件循环是否空闲；
  - `prepare`：为下一个阶段（`poll`）做准备工作。
- **阶段切换条件**：
  内部操作完成后，直接进入下一阶段（`poll`）。

#### 4. 阶段 4：`poll`（轮询阶段，核心 I/O 处理）

- **核心任务**：处理**用户主动注册的 I/O 回调**（最常用的阶段），包括：

  - 文件读写回调（`fs.readFile` 等）；
  - 网络请求回调（`http.createServer` 的 `request` 事件等）；
  - TCP 连接回调（`net.createServer` 的 `connection` 事件等）。

- **阶段内逻辑（关键）**：

  1. 先执行队列中所有 “已就绪的 I/O 回调”（即已完成的 I/O 操作对应的回调）；

  2. 若回调队列清空，进入 “等待状态”：

     - **如果存在 `setImmediate` 回调** → 跳过等待，直接进入 **check 阶段**。

       **如果不存在 `setImmediate` 回调** → 再看 timers：

       - **如果存在已到期的 timer** → poll 阶段结束，事件循环跳回 **timers 阶段**。
       - **如果没有到期的 timer** → **阻塞在 poll 阶段**，等待新的 I/O 事件到来（例如 socket 收到数据）。

  3. **一旦有新的 I/O 事件完成**，对应的回调会立即加入 poll 队列 → 下一轮循环继续执行。

- **阶段切换条件**：
  回调队列清空且无新 I/O 事件等待时，根据是否有 `setImmediate` 回调，跳至 `check` 阶段。

#### 5. 阶段 5：`check`（检查阶段）

- **核心任务**：专门执行 `setImmediate()` 注册的回调。
  - `setImmediate` 的设计目的是：让回调在 `poll` 阶段完成后 “立即执行”（无需等待下一轮 `timers` 阶段）。
- **阶段切换条件**：
  该阶段的回调队列清空后，进入下一阶段（`close callbacks`）。

#### 6. 阶段 6：`close callbacks`（关闭回调阶段）

- **核心任务**：执行资源关闭相关的回调，如：
  - `socket.on('close', ...)`（TCP 连接关闭回调）；
  - `http.Server.on('close', ...)`（HTTP 服务器关闭回调）。
- **阶段切换条件**：
  该阶段的回调队列清空后，**一轮事件循环结束**，回到第一个阶段（`timers`），开始下一轮循环。



##### 核心差异对比

| 版本范围           | 微任务执行时机                                               |
| ------------------ | ------------------------------------------------------------ |
| **Node 11 之前**   | 按 “**阶段**” 清空：一个阶段的所有宏任务执行完后，切换到下一阶段前，才清空 1 次微任务队列。 |
| **Node 11 及之后** | 按 “**单个宏任务**” 清空：每个宏任务执行完后，立即清空微任务队列，再执行下一个宏任务（与浏览器一致）。 |

#### 六、经典案例：理解执行顺序

通过代码示例可直观掌握事件循环的优先级，以下是常见场景的执行顺序分析：

##### 案例 1：Node11之前 vs Node11+

```javascript
// 两个 timers 阶段的宏任务（setTimeout），每个宏任务触发微任务（Promise.then）
setTimeout(() => {
  console.log('宏任务1（timers）');
  Promise.resolve().then(() => console.log('微任务1'));
}, 0);
setTimeout(() => {
  console.log('宏任务2（timers）');
  Promise.resolve().then(() => console.log('微任务2'));
}, 0);
```

**执行结果**：

```plaintext
// Node 11 之前（阶段级清空）：（先执行完 timers 阶段所有宏任务，再清微任务）
宏任务 1（timers）→ 宏任务 2（timers）→ 微任务 1 → 微任务 2

// Node 11+（任务级清空）：（每个宏任务后立即清微任务，与浏览器一致）
宏任务 1（timers）→ 微任务 1 → 宏任务 2（timers）→ 微任务 2
```

##### 案例 2：setTimeout vs setImmediate（顺序不确定）

```javascript
// 场景：在 I/O 回调中同时使用 setTimeout 和 setImmediate
const fs = require('fs');
fs.readFile(__filename, () => {
  // I/O 回调在 poll 阶段执行，执行完后进入 check 阶段
  setTimeout(() => console.log('setTimeout'), 0);
  setImmediate(() => console.log('setImmediate'));
});
```

**执行结果（顺序固定）**：

```plaintext
setImmediate  // 先执行 check 阶段
setTimeout    // 再回到 timers 阶段
```

**原因**：I/O 回调在 `poll` 阶段执行，执行完后会直接进入 `check` 阶段（`setImmediate` 的专属阶段），因此 `setImmediate` 先执行；而 `setTimeout` 需等待下一轮循环的 `timers` 阶段。

若不在 I/O 回调中（如主线程直接执行），`setTimeout` 和 `setImmediate` 的顺序**不确定**—— 因为 `setTimeout(0)` 实际最小延迟约 1ms，若事件循环启动速度快于 1ms，`timers` 阶段未到延迟时间，会先进入 `poll` 阶段，再执行 `setImmediate`；反之则 `setTimeout` 先执行。

#### 七、关键注意点

1. **process.nextTick 的 “插队” 特性**：
   它不属于任何阶段，会在**每个阶段切换前**强制清空队列，即使递归调用也会优先执行（可能导致事件循环阻塞，需避免深度递归）。
2. **线程池与 I/O 阻塞**：
   Node.js 主线程单线程，但 `libuv` 的线程池（默认 4 线程）处理耗时 I/O（如文件、DNS），因此 I/O 操作本身不阻塞主线程，仅回调在主线程执行。
3. **与浏览器事件循环的差异**：
   - 浏览器事件循环只有 “宏任务队列” 和 “微任务队列”，无阶段划分；
   - Node.js 有明确的 6 个阶段，且 `process.nextTick` 优先级高于浏览器的微任务。



### Node.js 与浏览器 `setTimeout` 的核心区别

两者的差异主要体现在 **最小延迟、执行环境、微任务处理** 三个方面：

| 对比维度       | Node.js `setTimeout`                                         | 浏览器 `setTimeout`                                          |
| -------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **最小延迟**   | 强制 ≥1ms（`0ms` 会被调整为 1ms）<br /><br />`setTimeout(fn, 0)` 等价于 `setTimeout(fn, 1)`（均为 1ms） | 通常 ≥4ms（受浏览器 “嵌套定时器” 规则影响，顶层调用可能更低） |
| **执行阶段**   | 属于 `timers` 阶段，与其他阶段（如 poll、check）严格按顺序执行 | 属于 “宏任务队列”，无明确阶段划分，执行后立即清空微任务      |
| **同批次处理** | 同一批到期的定时器会连续执行，微任务批量处理                 | 同一批到期的定时器会逐个执行，每个执行后立即清空微任务       |
| **底层依赖**   | 基于 libuv 库的定时器实现                                    | 基于浏览器引擎（如 V8+ 浏览器事件循环）                      |