

# Webpack & Babel 全维度解析笔记（面试 & 学习版）

## 目录

1. [Webpack 核心基础](https://www.doubao.com/chat/18759262286953986#1-webpack-核心基础)
2. [Webpack 构建流程与核心对象](https://www.doubao.com/chat/18759262286953986#2-webpack-构建流程与核心对象)
3. [Hash 类型辨析](https://www.doubao.com/chat/18759262286953986#3-hash-类型辨析)
4. [Loader 深度解析](https://www.doubao.com/chat/18759262286953986#4-loader-深度解析)
5. [Plugin 深度解析](https://www.doubao.com/chat/18759262286953986#5-plugin-深度解析)
6. [Tree-Shaking 原理与配置](https://www.doubao.com/chat/18759262286953986#6-tree-shaking-原理与配置)
7. [代码分割（splitChunks）](https://www.doubao.com/chat/18759262286953986#7-代码分割splitchunks)
8. [webpack-dev-server 配置与作用](https://www.doubao.com/chat/18759262286953986#8-webpack-dev-server-配置与作用)
9. [Webpack 开发 / 生产环境配置差异](https://www.doubao.com/chat/18759262286953986#9-webpack-开发生产环境配置差异)
10. [Babel 核心解析](https://www.doubao.com/chat/18759262286953986#10-babel-核心解析)
11. [Loader vs Plugin 核心区别](https://www.doubao.com/chat/18759262286953986#11-loader-vs-plugin-核心区别)
12. [Webpack 高频面试题与答案](https://www.doubao.com/chat/18759262286953986#12-webpack-高频面试题与答案)



## 1. Webpack 核心基础

### 1.1 定义与核心价值

- **本质**：现代前端 **模块打包器**，将项目中分散的模块（JS/CSS/ 图片 / 字体等）按依赖关系打包为浏览器可识别的静态资源。
- 核心解决的问题
  1. 模块依赖管理：处理 `import`/`require` 等模块引入方式，构建依赖图。
  2. 资源转换：通过 Loader 将非 JS 资源（如 CSS、TS）转为 JS 模块。
  3. 代码优化：压缩、Tree-Shaking、代码分割，减小包体积，提升加载速度。
  4. 开发效率：热更新、自动编译、错误提示。
- **局限性**：默认仅支持 JS/JSON 文件，其他资源需依赖 Loader 扩展。

### 1.2 核心概念

| 概念       | 定义                                                         |
| ---------- | ------------------------------------------------------------ |
| **Module** | 模块：项目中的单个文件（如 `index.js`、`style.css`），是 Webpack 打包的最小单位。 |
| **Chunk**  | 代码块：多个 Module 按依赖关系合并后的集合（如入口 chunk、公共 chunk、异步 chunk）。 |
| **Bundle** | 最终输出文件：Chunk 经过优化（压缩、Tree-Shaking）后生成的物理文件（如 `vendors.js`）。 |
| **Loader** | 转换器：将非 JS 模块转为 Webpack 可处理的 JS 模块（如 `css-loader` 处理 CSS）。 |
| **Plugin** | 扩展器：通过钩子干预构建流程，实现额外功能（如生成 HTML、压缩代码）。 |

## 2. Webpack 构建流程与核心对象

### 2.1 完整构建流程（生命周期）

Webpack 构建遵循 **“初始化 → 编译 → 输出”** 三阶段，每个阶段通过 **钩子（Hooks）** 暴露扩展点，插件可通过钩子干预流程。

| 阶段       | 核心操作                                                     | 关键钩子 / 对象                         | 面试应答要点                                                 |
| ---------- | ------------------------------------------------------------ | --------------------------------------- | ------------------------------------------------------------ |
| **初始化** | 1. 合并配置（webpack.config.js + 默认配置 + CLI 参数） 2. 创建 `Compiler` 对象（全局唯一） 3. 执行插件 `apply` 方法，注册钩子 | `environment`、`initialize`             | 强调 `Compiler` 是全局管理者，保存配置和钩子，整个构建只创建一次。 |
| **编译**   | 1. 解析入口文件（`entry`），生成 AST 2. 递归解析依赖，通过 Loader 转换模块 3. 收集依赖，构建依赖图 4. 创建 `Compilation` 对象（单次编译执行者） | `compile`、`compilation`、`buildModule` | 强调 `Compilation` 是单次编译的细节管理者，文件变化会新建实例，包含模块/chunk。 |
| **输出**   | 1. 按 `splitChunks` 拆分 Chunk 2. 执行优化（Tree-Shaking、压缩） 3. 将 Chunk 写入 `output.path`（如 `dist`） 4. 输出构建统计信息 | `emit`、`done`                          | 说明 `emit` 是输出前最后可干预的钩子，`done` 标志构建完成，可做日志统计。 |

- #### 完整顺序

  **一、初始化阶段（准备工作）**

  1. **参数合并**
     整合配置来源：`webpack.config.js` 配置文件、默认配置、CLI 命令参数（如 `--mode`、`--entry`），生成最终的配置对象。
  2. **创建 Compiler 对象**
     实例化全局唯一的 `Compiler` 对象（编译器），包含完整配置信息和钩子系统，贯穿整个构建生命周期。
     - `environment`：环境准备完成（如读取 `process.env`）。
     - `afterEnvironment`：环境准备后，插件可开始注册更细粒度的钩子。
  3. **注册插件**
     执行所有插件的 `apply(compiler)` 方法，插件通过 `compiler.hooks` 挂载钩子（此时尚未开始编译，仅完成插件注册）。
  4. **触发初始化钩子**
     - `initialize`：`Compiler` 对象初始化完成，准备进入编译阶段。

  **二、编译阶段（核心处理）**

  1. **启动编译**
     - 触发 `beforeRun`：编译开始前的准备（如检查缓存）。
     - 触发 `run`：正式启动编译流程。
  2. **创建 Compilation 对象** （`Compilation` 是单次编译的细节管理者，文件变化会新建实例，包含模块/chunk）
     - 触发 `beforeCompile`：编译前的最后准备（如清理临时文件）。
     - 触发 `compile`：开始创建 `Compilation` 对象（单次编译的执行者，包含当前编译的模块、chunk 等信息）。
     - 触发 `thisCompilation`：`Compilation` 对象刚创建时。
     - 触发 `compilation`：`Compilation` 对象初始化完成，可访问模块和 chunk 信息。
  3. **解析入口与模块** （只有当 Compilation 对象初始化完成（`compilation` 钩子触发后），Webpack 才会开始解析入口文件）
     - 从 `entry` 配置解析入口文件（如 `src/index.js`），生成抽象语法树（AST）。
     - 递归解析依赖：对每个模块执行 `buildModule` 钩子（可修改模块源码），通过 Loader 转换非 JS 模块（如 CSS → JS），收集依赖关系，构建 “模块依赖图”。
     - 触发 `succeedModule`/`failedModule`：单个模块构建成功 / 失败后。
  4. **模块优化**
     - 触发 `seal`：所有模块解析完成，准备生成 chunk。
     - 触发 `optimize`：开始优化流程（如 Tree-Shaking 标记未使用代码）。
     - 触发 `optimizeModules`：优化模块集合（合并、删除冗余模块）。
     - 触发 `optimizeChunks`：优化 chunk 集合（按 `splitChunks` 拆分 chunk）。

  **三、输出阶段（生成文件）**

  1. **资源优化**
     - 触发 `optimizeAssets`：优化输出资源（如压缩 JS/CSS、添加注释）。
     - 触发 `afterOptimizeAssets`：资源优化完成。
  2. **输出文件**
     - 触发 `afterSeal`：所有优化完成，准备输出到磁盘。
     - 触发 `emit`：输出前最后一步，可修改输出资源（如添加版权文件）。
     - Webpack 将 chunk 写入 `output.path` 目录（如 `dist`），生成最终文件（JS、CSS、图片等）。
     - 触发 `afterEmit`：文件输出完成，可做清理工作（如删除临时文件）。
  3. **完成构建**
     - 触发 `done`：整个构建流程结束，输出统计信息（耗时、文件列表、错误信息等）。

  **简化执行顺序总结**

  ```plaintext
  初始化阶段：
  参数合并 → 创建 Compiler → environment → afterEnvironment → 注册插件 → initialize → Compiler对象初始化完成
  
  编译阶段：
  beforeRun → run → beforeCompile → compile → thisCompilation → compilation → Compilation对象初始化完成
  buildModule（模块构建） → succeedModule → seal → optimize → optimizeModules → optimizeChunks
  
  输出阶段：
  optimizeAssets → afterOptimizeAssets → afterSeal → emit → 写入文件 → afterEmit → done
  ```

  **关键节点记忆点**

  - **Compiler 钩子**：全局流程控制（如 `run`、`done`），先于 `Compilation` 钩子执行。
  - **Compilation 钩子**：单次编译细节（如 `buildModule`、`emit`），在 `compilation` 钩子后开始执行。
  - **emit 钩子**：输出前最后可干预的节点，常用于修改输出资源（如添加构建信息文件）。
  - **done 钩子**：构建完全结束，可用于输出构建日志或发送通知。

### 2.2 核心对象：Compiler vs Compilation

| 对比项       | Compiler                                                     | Compilation                                                  |
| ------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **生命周期** | 全局唯一，构建全程只实例化 1 次，插件挂载在 `compiler.hooks` | 单次编译实例，文件变化触发重新编译时新建，插件挂载在 `compilation.hooks` |
| **核心作用** | 管理全局配置、钩子系统，调度构建流程                         | 管理当前编译的模块、chunk、资源，执行具体编译逻辑            |
| **钩子范围** | 全局钩子（如 `run`、`done`）                                 | 模块级钩子（如 `buildModule`、`optimizeAssets`）             |
| **数据存储** | 保存全局配置（`entry`、`output` 等）                         | 保存当前编译的模块依赖图、chunk 列表、资源列表               |

**面试记忆技巧**：`Compiler` 是 “导演”（管全局流程），`Compilation` 是 “场记”（管单次拍摄细节）。

### 2.3 `Compiler` 生命周期钩子（全局流程）

`Compiler` 的钩子贯穿整个构建生命周期，插件通过 `compiler.hooks.xxx.tap()` 注册逻辑，关键钩子按执行顺序如下：

| **钩子名称**       | **触发时机**                           | **参数说明**                                       | **典型作用**                                                 | **使用频率** |
| ------------------ | -------------------------------------- | -------------------------------------------------- | ------------------------------------------------------------ | ------------ |
| **初始化阶段**     |                                        |                                                    |                                                              |              |
| `environment`      | `Compiler` 初始化，环境变量加载完成    | 无参数                                             | 读取 `process.env` 配置，初始化插件基础环境（如判断开发 / 生产环境） | 中           |
| `afterEnvironment` | `environment` 后，插件可注册钩子       | 无参数                                             | 基于环境变量注册条件钩子（如生产环境才启用压缩插件）         | 中           |
| `initialize`       | `Compiler` 配置初始化完成              | 无参数                                             | 初始化插件内部状态（如缓存容器、计数器、临时文件路径）       | 低           |
| **编译阶段**       |                                        |                                                    |                                                              |              |
| `beforeRun`        | 编译开始前（`compiler.run()` 调用前）  | `compiler`（当前编译器实例）                       | 清理上一次构建缓存、检查前置依赖（如 node_modules 是否完整） | 中           |
| `run`              | 正式启动编译流程                       | `compiler`、`callback`（异步回调）                 | 记录构建开始时间、启动文件监听（如 `watch` 模式初始化）      | 中           |
| `beforeCompile`    | 准备创建 `Compilation` 前              | `params`（编译参数，含 `compilationDependencies`） | 动态修改入口配置（如根据环境添加 `entry`）、补充编译依赖     | 高           |
| `compile`          | 开始创建 `Compilation` 对象            | `params`（同 `beforeCompile`）                     | 记录编译参数（如入口文件列表）、准备编译资源（如内存缓存初始化） | 中           |
| `thisCompilation`  | `Compilation` 刚创建时                 | `compilation`（新创建的编译实例）                  | 初始化与当前编译强相关的逻辑（如为本次编译单独设置临时目录） | 低           |
| `compilation`      | `Compilation` 初始化完成               | `compilation`（初始化后的编译实例）                | 注册 `Compilation` 阶段的钩子（如 `buildModule`/`optimizeAssets`），是插件介入单次编译的核心入口 | **高频**     |
| `afterCompile`     | 单次编译流程结束（`Compilation` 完成） | `compilation`                                      | 验证编译结果（如检查输出文件大小是否超标）、记录模块依赖关系 | 中           |
| **输出阶段**       |                                        |                                                    |                                                              |              |
| `emit`             | 资源输出到 `dist` 前（写入磁盘前）     | `compilation`                                      | 最后修改输出资源（如给 JS/CSS 添加版权注释、动态生成 `version.txt`） | **高频**     |
| `afterEmit`        | 资源输出到 `dist` 后（写入磁盘后）     | `compilation`                                      | 清理临时文件、发送构建完成通知（如邮件 / 钉钉消息）、部署输出资源到测试环境 | 中           |
| `done`             | 整个构建流程完全结束                   | `stats`（构建统计信息对象）                        | 输出构建报告（耗时、文件列表、错误数）、生成性能分析日志     | **高频**     |
| `failed`           | 构建失败时（任何阶段抛出错误）         | `error`（错误对象）                                | 捕获构建错误、输出友好提示（如 “请检查 Loader 配置”）、发送错误告警 | 中           |

**面试高频**：`emit`（输出前修改资源）、`done`（构建后日志）、`compilation`（干预编译细节）。

### 2.4 `Compilation` 生命周期钩子（单次编译细节）

`Compilation` 的钩子聚焦于单次编译的具体环节，插件通过 `compilation.hooks.xxx.tap()` 注册逻辑，关键钩子按执行顺序如下：

| **钩子名称**          | **触发时机**                                | **参数说明**                         | **典型作用**                                                 | **使用频率** |
| --------------------- | ------------------------------------------- | ------------------------------------ | ------------------------------------------------------------ | ------------ |
| `buildModule`         | 模块开始构建（Loader 处理前）               | `module`（当前模块实例）             | 修改模块源码（如注入全局变量 `__VERSION__`）、跳过不必要的模块（如 mock 文件） | 高           |
| `rebuildModule`       | 模块重新构建（文件变化触发）                | `module`                             | 增量编译时，仅处理修改的模块（如记录模块修改时间）           | 中           |
| `succeedModule`       | 模块构建成功（Loader 处理完成）             | `module`                             | 记录模块信息（大小、依赖路径）、验证 Loader 转换结果（如 CSS 是否正确解析） | 中           |
| `failedModule`        | 模块构建失败                                | `module`、`error`                    | 捕获模块错误（如 `sass-loader` 语法错误）、输出模块路径便于定位问题 | 中           |
| `seal`                | 所有模块解析完成，准备生成 chunk            | 无参数                               | 锁定模块依赖图（禁止再添加模块）、初始化 chunk 生成规则      | 高           |
| `optimize`            | 开始优化流程                                | 无参数                               | 初始化优化工具（如 Tree-Shaking 分析器、代码压缩器）         | 中           |
| `optimizeModules`     | 优化模块集合                                | `modules`（当前所有模块数组）        | 删除未使用模块（Tree-Shaking 核心阶段）、合并重复模块（如相同工具函数） | 高           |
| `optimizeChunks`      | 优化 chunk 集合                             | `chunks`（当前所有 chunk 数组）      | 按 `splitChunks` 拆分公共 chunk、合并小 chunk（减少 HTTP 请求） | 高           |
| `optimizeChunkAssets` | 优化 chunk 生成的资源（如 JS）              | `chunks`                             | 压缩 JS 代码（如 `Terser` 处理）、添加 chunk 注释（如 `// vendor chunk`） | 高           |
| `optimizeAssets`      | 优化所有输出资源（JS/CSS/ 图片等）          | `assets`（资源键值对，key 为文件名） | 全局资源优化（如给所有文件添加哈希、压缩 CSS / 图片）        | 高           |
| `afterOptimizeAssets` | 资源优化完成                                | `assets`                             | 验证优化结果（如压缩率是否达标）、记录资源最终大小           | 中           |
| `moduleAsset`         | 模块生成资源时（如 `file-loader` 输出图片） | `module`、`filename`（资源文件名）   | 记录模块与资源的映射关系（如 “index.js → icon.png”）         | 低           |
| `chunkAsset`          | chunk 生成资源时（如入口 chunk 输出 JS）    | `chunk`、`filename`                  | 记录 chunk 与资源的映射关系（如 “main chunk → main.xxx.js”） | 低           |
| `afterSeal`           | 所有优化完成，准备输出                      | 无参数                               | 最终调整输出路径、确认 chunk 依赖关系                        | 中           |

### 2.5 总结

- **Compiler 钩子**：控制全局流程，从初始化到构建结束，`compilation`/`emit`/`done` 是高频使用点。
- **Compilation 钩子**：干预单次编译细节，`buildModule`/`optimizeAssets` 用于修改模块和优化资源。

## 3. Hash 类型辨析

Hash 的核心作用是 **通过文件名变化触发浏览器缓存更新**，三种 Hash 类型的关键区别在于 “计算维度”。

| 类型          | 计算维度             | 特点                                         | 适用场景                      | 面试易错点                                                   |
| ------------- | -------------------- | -------------------------------------------- | ----------------------------- | ------------------------------------------------------------ |
| `hash`        | 整个项目的构建版本   | 任一文件修改，所有文件的 `hash` 全变         | 单文件项目（几乎不用）        | 多文件项目用 `hash` 会导致缓存失效                           |
| `chunkhash`   | 单个 Chunk（代码块） | 同 Chunk 内文件共享 `hash`，Chunk 不变则不变 | JS 入口文件、代码分割的 Chunk | 同一 Chunk 内文件联动（如 JS 改了，同 Chunk 的 CSS `hash` 变） |
| `contenthash` | 单个文件的内容       | 仅文件自身修改时 `hash` 变，完全独立         | 提取的 CSS、图片、字体        | 必须配合 `MiniCssExtractPlugin` 使用，否则无法单独生成 CSS 的 `contenthash` |

**面试场景题**：为什么 CSS 文件要用 `contenthash`？
答：因为 CSS 若用 `chunkhash`，当同 Chunk 的 JS 文件修改时，CSS 的 `chunkhash` 会同步变化，导致 CSS 缓存失效；而 `contenthash` 只依赖 CSS 自身内容，JS 修改不影响 CSS 缓存。

## 4. Loader 深度解析

### 4.1 定义与核心作用

- **本质**：一个 **函数**，输入 “源文件内容”，转为js输出。
- **核心逻辑**：`(source: string) => transformedSource: string`（链式调用，前一个 Loader 的输出是后一个的输入）。

### 4.2 执行顺序

**规则**：**右到左、下到上**（链式调用，从最后一个 Loader 开始处理源码）。
示例：处理 SCSS 文件的 Loader 配置：

```javascript
module: {
  rules: [
    {
      test: /\.scss$/,
      use: [
        'style-loader',   // 3. 将 CSS 注入 DOM（<style> 标签）
        'css-loader',     // 2. 解析 CSS 中的 @import 和 url()
        'sass-loader'     // 1. 将 SCSS 编译为 CSS（先处理源码）
      ]
    }
  ]
}
```

**面试记忆技巧**：`use` 数组的顺序是 “最终处理步骤在前，初始处理步骤在后”，如 SCSS 需先转 CSS（`sass-loader`），再解析依赖（`css-loader`），最后注入 DOM（`style-loader`）。

### 4.3 核心 API 与使用场景

| API 名称            | 类型 | 作用                           | 使用场景示例                                                 |
| ------------------- | ---- | ------------------------------ | ------------------------------------------------------------ |
| `this.async()`      | 方法 | 声明 Loader 为异步模式         | 处理异步操作（如读取远程配置文件）                           |
| `this.callback()`   | 方法 | 返回多结果（源码 + SourceMap） | 生成 SourceMap，方便调试（如 `babel-loader` 输出源码和映射） |
| `this.emitFile()`   | 方法 | 生成独立输出文件               | `file-loader` 处理图片，输出到 `images` 目录                 |
| `this.getOptions()` | 方法 | 安全获取 Loader 配置           | 读取用户配置的选项（如 `url-loader` 的 `limit` 参数）        |
| `this.cacheable()`  | 方法 | 控制缓存（默认开启）           | 动态依赖文件（如配置文件）修改时，禁用缓存，强制重新编译     |
| `this.resourcePath` | 属性 | 获取当前处理文件路径           | 给源码添加文件路径注释（如 `/* 来自 src/style.scss */`）     |

**面试示例**：自定义一个简单的 Loader，给 JS 文件添加版权注释：

```javascript
// copyright-loader.js
module.exports = function (source) {
  const copyright = `/* 版权所有 © 2024 */\n`;
  return copyright + source; // 输出：版权注释 + 原源码
};

// Webpack 配置
module: {
  rules: [{ test: /\.js$/, use: './copyright-loader.js' }]
}
```

### 4.4 常用 Loader 速查

| Loader 名称    | 作用                               | 关键配置选项                                  |
| -------------- | ---------------------------------- | --------------------------------------------- |
| `babel-loader` | 将 ES6+ 转为 ES5                   | `presets: ['@babel/preset-env']`              |
| `css-loader`   | 解析 CSS 中的 `@import` 和 `url()` | `modules: true`（启用 CSS Modules）           |
| `style-loader` | 将 CSS 注入 DOM（<style> 标签）    | `injectType: 'singletonStyleTag'`（单标签）   |
| `sass-loader`  | 将 SCSS/SASS 转为 CSS              | `additionalData: '$color: #fff;'`（全局变量） |
| `url-loader`   | 处理图片 / 字体，小文件转 Base64   | `limit: 8 * 1024`（＜8kb 转 Base64）          |
| `file-loader`  | 处理图片 / 字体，输出为独立文件    | `name: 'assets/[name].[hash].[ext]'`          |
| `ts-loader`    | 将 TypeScript 转为 JS              | `transpileOnly: true`（加速构建）             |

## 5. Plugin 深度解析

### 5.1 定义与核心作用

- **本质**：一个 **包含 `apply(compiler)` 方法的类 / 对象**，通过 Webpack 的 **钩子系统** 干预构建流程，实现 **扩展功能**（如生成 HTML、压缩代码、注入变量）。
- **核心逻辑**：在 `apply` 方法中，通过 `compiler.hooks.xxx.tap()` 注册钩子回调，在特定阶段执行自定义逻辑。

### 5.2 核心 API 与注册方式

Webpack 依赖 `tapable` 库实现钩子系统，插件注册钩子的三种方式：

| **执行逻辑**                               | 注册方法               | 作用场景                                 | 示例代码                                                     | **关键说明**                                                 |
| ------------------------------------------ | ---------------------- | ---------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| 同步执行，所有回调按顺序完成               | `tap(name, fn)`        | 同步钩子：回调执行完后，Webpack 继续流程 | `compiler.hooks.done.tap('MyPlugin', (stats) => { console.log('构建完成'); })` | - `name`：插件名称（调试用，需唯一） - `fn`：同步回调，无返回值 - 适用于同步钩子（如 `done`/`compilation`） |
| 异步串行，前一个回调完成后执行下一个       | `tapAsync(name, fn)`   | 异步钩子：回调通过 `callback()` 通知完成 | `compiler.hooks.emit.tapAsync('MyPlugin', (compilation, callback) => { /* 处理后调用 callback() */ })` | - `fn` 最后一个参数为 `callback`，必须调用（`callback(err)` 传递错误） - 适用于异步串行钩子（如 `emit`/`run`） |
| 异步并行，所有回调同时执行，全部完成后继续 | `tapPromise(name, fn)` | 异步钩子：回调返回 Promise 通知完成      | `compiler.hooks.emit.tapPromise('MyPlugin', async (compilation) => { /* 异步操作 */ })` | - `fn` 需返回 Promise，resolve 后触发下一步 - 适用于异步钩子，支持 `async/await` 语法（推荐） |

**面试注意**：异步钩子必须通过 `callback()` 或 Promise 结束，否则 Webpack 会卡住。

### 5.3 自定义 Plugin 示例（面试手写）

需求：构建完成后，在 `dist` 目录生成 `build-info.txt`，记录构建时间和文件列表。

```javascript
// BuildInfoPlugin.js
class BuildInfoPlugin {
  apply(compiler) {
    // 注册 done 钩子（构建完成后执行）
    compiler.hooks.done.tap('BuildInfoPlugin', (stats) => {
      const { compilation } = stats;
      // 1. 收集输出文件列表
      const outputFiles = Object.keys(compilation.assets);
      // 2. 构建信息内容
      const content = `
        构建时间：${new Date().toLocaleString()}
        输出文件：${outputFiles.join('\n  - ')}
        构建状态：成功
      `;
      // 3. 生成 build-info.txt 并添加到输出资源
      compilation.assets['build-info.txt'] = {
        source: () => content, // 资源内容
        size: () => content.length // 资源大小（Webpack 需计算）
      };
    });
  }
}

module.exports = BuildInfoPlugin;

// Webpack 配置中使用
plugins: [new BuildInfoPlugin()]
```

**面试加分点**：提到 `compilation.assets` 是输出资源的集合，可通过修改它添加 / 删除输出文件。

```js
// Rsbuild 插件
import fs from 'node:fs';
import path from 'node:path';
export const BuildInfoPlugin = () => {
  return {
    name: 'build-info-plugin',
    setup(api) {
      api.onAfterBuild(async ({ stats }) => {
        // 1. 收集输出文件
        const outputFiles = Object.keys(stats?.compilation?.assets ?? {});
        // 2. 构建信息内容
        const content = `
            构建时间：${new Date().toLocaleString()}
            输出文件：
              - ${outputFiles.join('\n  - ')}
            构建状态：成功
        `;
        // 3. 输出 build-info.txt 文件
        const outputPath = stats?.compilation?.outputOptions?.path || process.cwd();
        const filePath = path.join(outputPath, 'build-info.txt');
        // 写入构建产物目录
        fs.writeFileSync(filePath, content, 'utf-8');
      });
    },
  };
};
```

### 5.4 常用 Plugin 速查

| Plugin 名称             | 作用                                      | 关键配置选项                                                 |
| ----------------------- | ----------------------------------------- | ------------------------------------------------------------ |
| `HtmlWebpackPlugin`     | 自动生成 HTML 文件，引入打包后的 JS/CSS   | `template: './public/index.html'`（模板文件）                |
| `MiniCssExtractPlugin`  | 将 CSS 从 JS 中提取为独立文件             | `filename: 'css/[name].[contenthash].css'`                   |
| `CleanWebpackPlugin`    | 构建前自动清理 `dist` 目录                | `cleanOnceBeforeBuildPatterns: ['**/*']`                     |
| `DefinePlugin`          | 注入全局变量（如 `process.env.NODE_ENV`） | `{ 'process.env.NODE_ENV': JSON.stringify(process.env.NODE_ENV) }` |
| `TerserWebpackPlugin`   | 压缩 JS 代码（Webpack5 生产模式默认）     | `parallel: true`（多线程压缩，加速）                         |
| `CssMinimizerPlugin`    | 压缩 CSS 代码                             | `parallel: true`（多线程压缩）                               |
| `WebpackBundleAnalyzer` | 可视化分析包体积，定位大依赖              | `openAnalyzer: true`（构建后自动打开）                       |

## 6. Tree-Shaking 原理与配置

### 6.1 定义与核心目标

- **本质**：移除代码中 **未被使用的死代码（dead code）**，减小包体积。
- **核心依赖**：**ES 模块（ESM）的静态导入 / 导出**（`import`/`export`），因为 ESM 是 “静态的”（编译时可确定依赖关系），而 CommonJS（`require`）是 “动态的”（运行时才能确定，无法静态分析）。

### 6.2 生效三条件（面试必答）

1. **模块系统**：代码必须使用 ESM（`import`/`export`），禁止转为 CommonJS（需配置 Babel 的 `modules: false`）。
2. **模式**：`mode: 'production'`（开发模式默认关闭优化，`usedExports: false`）。
3. **副作用声明**：在 `package.json` 中配置 `sideEffects`，告知 Webpack 哪些文件有 “副作用”（不能删除），哪些没有（可安全 Tree-Shaking）。

**副作用**：指代码执行时会影响外部环境（如修改全局变量、添加 DOM 事件、引入 CSS），这类代码即使未被直接引用，也不能删除。

### 6.3 关键配置

#### （1）Webpack 配置

```javascript
optimization: {
  usedExports: true, // 标记未使用的导出（开发模式可开启用于调试）
  minimize: true,    // 生产模式默认开启，删除标记的死代码（依赖 TerserWebpackPlugin）
  concatenateModules: true // 开启 Scope Hoisting，合并模块到一个函数中，减少代码体积
}
```

#### （2）package.json 配置 `sideEffects`

```json
{
  "sideEffects": [
    "*.css",          // CSS 文件有副作用（全局样式），不能删除
    "src/utils/logger.js", // 日志工具会修改全局 console，有副作用
    "!src/utils/helper.js" // 否定：helper.js 无副作用，可安全 Tree-Shaking
  ]
  // 若所有文件均无副作用，可直接设为 "sideEffects": false（需谨慎，避免误删有副作用的代码）
}
```

### 6.4 面试高频问题：Tree-Shaking 不生效的原因

1. 代码使用 CommonJS 模块（`require`/`module.exports`），而非 ESM。
2. Babel 配置中 `@babel/preset-env` 的 `modules` 设为 `commonjs`（默认是 `false`，需保持 `false`）。
3. 未配置 `sideEffects`，Webpack 为避免误删有副作用的代码，不执行 Tree-Shaking。
4. 开发模式（`mode: 'development'`）下，`minimize: false`，未删除标记的死代码。

## 7. 代码分割（splitChunks）

### 7.1 定义与核心目标

- **本质**：将大 Chunk 拆分为多个小 Chunk，实现 **“按需加载”**（减少首屏加载体积）和 **“长期缓存”**（第三方库单独打包，业务代码修改不影响其缓存）。
- 核心场景
  1. 提取第三方库（如 `lodash`、`React`）到 `vendors.js`。
  2. 提取多入口共享的业务代码到 `common.js`。
  3. 异步加载模块（如路由懒加载）自动拆分为独立 Chunk。

### 7.2 splitChunks 核心配置（Webpack5 默认）

```javascript
optimization: {
  splitChunks: {
    chunks: 'all', // 对所有类型 Chunk 生效（同步 + 异步），推荐
    minSize: 20000, // 拆分的 Chunk 最小体积（20kb），小于则不拆分
    minRemainingSize: 0, // 拆分后剩余体积最小限制（避免拆分过小的 Chunk）
    minChunks: 1, // 一个模块被至少引用多少次才会被拆分
    maxAsyncRequests: 30, // 异步加载时最大并行请求数
    maxInitialRequests: 30, // 入口 Chunk 最大并行请求数
    enforceSizeThreshold: 50000, // 超过 50kb 的 Chunk 强制拆分，忽略上述限制
    cacheGroups: { // 缓存组：按规则拆分 Chunk
      vendors: { // 缓存组 1：提取第三方库
        test: /[\\/]node_modules[\\/]/, // 匹配 node_modules 中的模块
        priority: -10, // 优先级（数值越高越先匹配）
        reuseExistingChunk: true, // 复用已存在的 Chunk，避免重复拆分
        name: 'vendors' // 输出 Chunk 名称：vendors.js
      },
      common: { // 缓存组 2：提取共享业务代码
        name: 'common',
        minChunks: 2, // 至少被 2 个模块引用才拆分
        priority: -20,
        reuseExistingChunk: true
      }
    }
  },
  runtimeChunk: 'single' // 将 Webpack 运行时代码（如模块加载逻辑）拆分为单独 Chunk（runtime.js）
}
```

**面试重点**：

- `chunks: 'all'`：兼顾同步和异步 Chunk（如异步路由的共享模块也会被提取）。
- `cacheGroups.vendors`：通过 `test: /node_modules/` 匹配第三方库，单独打包以利用缓存。
- `runtimeChunk`：避免业务代码修改导致第三方库 Chunk 的 `hash` 变化（运行时代码单独打包）。

### 7.3 chunks 选项：initial vs async vs all

| 取值      | 作用范围                      | 适用场景                                                     |
| --------- | ----------------------------- | ------------------------------------------------------------ |
| `async`   | 仅拆分异步 Chunk（默认值）    | 单页应用路由懒加载，只优化异步加载模块                       |
| `initial` | 仅拆分同步入口 Chunk          | 多入口应用（如 `entry: { page1: './page1.js' }`），提取入口共享代码 |
| `all`     | 拆分所有 Chunk（同步 + 异步） | 所有场景推荐，最大化代码复用和缓存效率                       |

**面试示例**：多入口应用（page1.js 和 page2.js 都引用 `lodash`），用 `chunks: 'initial'` 会提取 `lodash` 到 `common.js`；若其中一个入口异步加载 `module.js` 也引用 `lodash`，则需 `chunks: 'all'` 才能将 `lodash` 统一提取到 `vendors.js`。

## 8. webpack-dev-server 配置与作用

### 8.1 核心作用

- 开发环境专用服务器，解决 “开发时手动编译、刷新页面” 的低效问题，核心功能：
  1. **自动编译**：监听源文件变化，修改后自动重新编译。
  2. **热模块替换（HMR）**：只更新修改的模块，不刷新整个页面（如修改 CSS 实时生效，JS 模块热更新）。
  3. **代理跨域**：配置 `proxy` 解决开发环境跨域问题（如请求后端接口）。
  4. **静态文件服务**：提供 `contentBase` 目录的静态文件访问（如 `public` 目录的 `index.html`）。

### 8.2 核心配置

```javascript
devServer: {
  port: 3000, // 服务器端口
  open: true, // 启动后自动打开浏览器
  hot: true, // 开启热模块替换（HMR）
  compress: true, // 开启 Gzip 压缩，提升加载速度
  static: {
    directory: path.join(__dirname, 'public') // 静态文件目录（如 index.html、图片）
  },
  proxy: { // 代理跨域配置
    '/api': { // 匹配以 /api 开头的请求
      target: 'http://localhost:8080', // 后端接口地址
      changeOrigin: true, // 开启跨域（修改请求头的 Host 为 target）
      pathRewrite: { '^/api': '' } // 重写路径：/api/login → /login
    }
  },
  historyApiFallback: true // 支持 SPA 路由（如 React Router 的 BrowserRouter），404 时返回 index.html
}
```

**面试高频**：

- `hot: true`：需配合 `HotModuleReplacementPlugin`（Webpack5 已内置，无需手动引入）。
- `proxy.changeOrigin: true`：解决跨域的核心配置，模拟同源请求。
- `historyApiFallback: true`：解决 SPA 路由刷新 404 问题（服务器将所有路由指向 `index.html`）。

## 9. Webpack 开发 / 生产环境配置差异

| 配置项                  | 开发环境（development）                           | 生产环境（production）                                       | 原因分析                                                     |
| ----------------------- | ------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| `mode`                  | `development`                                     | `production`                                                 | 开发模式：关闭压缩、保留 sourceMap、开启 HMR；生产模式：开启压缩、Tree-Shaking |
| `devtool`               | `eval-cheap-module-source-map`                    | `source-map` 或 `hidden-source-map`                          | 开发模式：快速构建、保留行号；生产模式：完整 sourceMap（用于调试）或隐藏（避免暴露源码） |
| `output.filename`       | `[name].js`                                       | `[name].[contenthash:8].js`                                  | 生产模式用 `contenthash` 优化缓存；开发模式无需哈希，加快构建 |
| `optimization.minimize` | `false`                                           | `true`（默认）                                               | 生产模式开启 JS/CSS 压缩，减小包体积；开发模式关闭以加快构建 |
| `plugins`               | `HotModuleReplacementPlugin`、`HtmlWebpackPlugin` | `MiniCssExtractPlugin`、`CssMinimizerPlugin`、`CleanWebpackPlugin` | 生产模式需提取 CSS、压缩、清理 dist；开发模式需 HMR 提升效率 |
| `devServer`             | 配置（port、proxy、hot 等）                       | 不配置（生产环境用 Nginx 等服务器部署）                      | 开发模式需实时预览；生产模式无需开发服务器                   |

**面试技巧**：用 `webpack-merge` 拆分配置（公共配置 + 开发配置 + 生产配置），避免重复代码：

```javascript
// webpack.common.js（公共配置）
module.exports = { entry: './src/index.js', plugins: [new HtmlWebpackPlugin()] };

// webpack.dev.js（开发配置）
const { merge } = require('webpack-merge');
const common = require('./webpack.common.js');
module.exports = merge(common, { mode: 'development', devServer: { port: 3000 } });

// webpack.prod.js（生产配置）
module.exports = merge(common, { mode: 'production', plugins: [new MiniCssExtractPlugin()] });
```

## 10. Babel 核心解析

### 10.1 定义与核心价值

- **本质**：JavaScript 编译器（工具链），将 **ES6+ 语法**（如箭头函数、`Promise`）转为 **ES5 语法**，解决旧浏览器兼容性问题。
- **核心区别**：Babel 只转 “语法”（如 `() => {}` → `function() {}`），不转 “API”（如 `Promise`、`Array.prototype.includes`），API 兼容需靠 **polyfill**（如 `core-js`）。

### 10.2 核心流程：三阶段处理

Babel 编译本质是 **操作抽象语法树（AST）**，流程分为 “解析 → 转换 → 生成”。

| 阶段                  | 核心工具                 | 作用                                                         |
| --------------------- | ------------------------ | ------------------------------------------------------------ |
| **解析（Parse）**     | `@babel/parser`          | 将源码字符串转为 AST（抽象语法树），标记语法结构（如变量、函数、表达式）。 |
| **转换（Transform）** | `@babel/traverse` + 插件 | 遍历 AST，按插件 / 预设规则修改节点（如将箭头函数节点转为普通函数节点）。 |
| **生成（Generate）**  | `@babel/generator`       | 将修改后的 AST 转为 ES5 代码字符串，并生成 SourceMap（用于调试）。 |

**面试可视化工具**：推荐 `https://astexplorer.net/`（输入 ES6 代码，实时查看 AST 和转换后的 ES5 代码）。

### 10.3 核心依赖库速查

| 库名称                            | 作用                                      | 面试出现频率 |
| --------------------------------- | ----------------------------------------- | ------------ |
| `@babel/core`                     | Babel 核心编译器，调度三阶段流程          | 高频         |
| `@babel/parser`                   | 解析源码为 AST                            | 中频         |
| `@babel/traverse`                 | 遍历并修改 AST                            | 中频         |
| `@babel/generator`                | AST 转为 ES5 代码                         | 中频         |
| `@babel/preset-env`               | 预设：自动根据目标浏览器选择转换插件      | 高频         |
| `@babel/plugin-transform-runtime` | 复用辅助函数，避免代码冗余                | 高频         |
| `core-js`                         | 提供 ES6+ API 的 polyfill（如 `Promise`） | 高频         |
| `regenerator-runtime`             | 提供 `async/await` 语法的 polyfill        | 中频         |

### 10.4 关键配置：preset 与 polyfill

#### （1）基础配置：.babelrc 或 babel.config.json

```json
{
  "presets": [
    [
      "@babel/preset-env", // 核心预设：自动处理 ES6+ 语法
      {
        "targets": "> 0.25%, not dead", // 目标浏览器（兼容全球 0.25% 以上使用率、非停止维护的浏览器）
        "useBuiltIns": "usage", // 自动按需引入 polyfill（推荐）
        "corejs": 3, // 指定 core-js 版本（必须安装对应版本：npm i core-js@3）
        "modules": false // 保留 ESM 语法，支持 Webpack Tree-Shaking（关键！）
      }
    ]
  ],
  "plugins": [
    "@babel/plugin-transform-runtime" // 复用辅助函数（如 _classCallCheck），减少代码体积
  ]
}
```

#### （2）polyfill 配置：useBuiltIns 的三种取值

polyfill 用于补充旧浏览器缺失的 ES6+ API（如 `Promise`、`Array.prototype.flat`），`useBuiltIns` 控制 polyfill 的引入方式：

| 取值    | 作用                                                         | 优缺点                                                | 适用场景                     |
| ------- | ------------------------------------------------------------ | ----------------------------------------------------- | ---------------------------- |
| `usage` | 自动分析代码，按需引入用到的 polyfill                        | 优点：体积最小；缺点：需安装 core-js@3                | 生产环境（推荐）             |
| `entry` | 入口文件手动引入 `core-js`，全量引入目标浏览器缺失的 polyfill | 优点：配置简单；缺点：体积大（引入未用到的 polyfill） | 快速开发、对体积不敏感的场景 |
| `false` | 不自动引入 polyfill，需手动按需引入                          | 优点：完全可控；缺点：手动维护成本高                  | 对 polyfill 有严格控制的场景 |

**面试易错点**：`useBuiltIns: "usage"` 不需要在入口文件手动引入 `core-js`，Babel 会自动分析代码并注入所需 polyfill；`modules: false` 必须设置，否则 Babel 会将 ESM 转为 CommonJS，导致 Webpack Tree-Shaking 失效。

### 10.5 与 Webpack 配合：babel-loader

在 Webpack 中通过 `babel-loader` 处理 JS 文件，配置如下：

```javascript
module: {
  rules: [
    {
      test: /\.m?js$/, // 匹配 JS 文件（.mjs 是 ESM 专用扩展名）
      exclude: /node_modules/, // 排除 node_modules（第三方库通常已处理兼容性）
      use: {
        loader: 'babel-loader',
        options: {
          cacheDirectory: true // 开启缓存，加速二次构建（将编译结果缓存到 node_modules/.cache/babel-loader）
        }
      }
    }
  ]
}
```

**面试注意**：`exclude: /node_modules/` 是性能优化，避免重复编译第三方库；`cacheDirectory: true` 可大幅提升二次构建速度。

## 11. Loader vs Plugin 核心区别

| 对比项       | Loader                                              | Plugin                                                       |
| ------------ | --------------------------------------------------- | ------------------------------------------------------------ |
| **本质**     | 文件转换器（函数）                                  | 构建流程扩展器（含 `apply` 方法的类）                        |
| **核心作用** | 解决 “Webpack 不能处理非 JS 资源”                   | 解决 “构建流程需要扩展”（如生成 HTML、压缩）                 |
| **作用时机** | 模块加载阶段（编译前，处理源码）                    | 整个生命周期（如编译前、输出前、构建后）                     |
| **作用范围** | 单个文件类型（如 `.js`、`.css`）                    | 全局构建流程（影响所有模块 / 资源）                          |
| **执行顺序** | 右到左、下到上（链式调用）                          | 同钩子按注册顺序，不同钩子按生命周期顺序                     |
| **输入输出** | 输入：源文件内容 → 输出：转换后代码                 | 无固定输入输出，通过钩子操作 compiler/compilation            |
| **示例**     | `babel-loader`（转 ES6+）、`css-loader`（解析 CSS） | `HtmlWebpackPlugin`（生成 HTML）、`MiniCssExtractPlugin`（提取 CSS） |

**面试记忆类比**：

- Loader 是 “翻译员”：把 “外语文件”（CSS、TS）翻译成 Webpack 能懂的 “中文文件”（JS 模块）。
- Plugin 是 “导演助理”：在 “拍摄流程”（构建）的各个阶段（开机、拍摄、杀青）提供支持（如准备道具、记录场记、整理成片）。

## 12. Webpack 高频面试题与答案

### 12.1 如何在 Webpack 中处理图片资源？

**核心思路**：用 `url-loader`（包含 `file-loader` 功能），小图片转 Base64 减少 HTTP 请求，大图片单独输出。

**配置示例**：

```javascript
module: {
  rules: [
    {
      test: /\.(png|jpg|jpeg|gif|svg)$/i,
      type: 'asset', // Webpack5 新语法，替代 url-loader/file-loader
      parser: {
        dataUrlCondition: {
          maxSize: 8 * 1024 // ＜8kb 的图片转 Base64
        }
      },
      generator: {
        filename: 'images/[name].[contenthash:8].[ext]' // 大图片输出路径
      }
    }
  ]
}
```

**面试加分点**：提到 Webpack5 用 `type: 'asset'` 替代 `url-loader`，配置更简洁；`contenthash` 确保图片不变时缓存生效。

### 12.2 如何抽离 CSS 为单独文件，而非注入到 JS 中？

**核心思路**：用 `MiniCssExtractPlugin` 替代 `style-loader`，将 CSS 从 JS 中提取为独立文件，配合 `CssMinimizerPlugin` 压缩 CSS。

**配置示例**：

```javascript
const MiniCssExtractPlugin = require('mini-css-extract-plugin');
const CssMinimizerPlugin = require('css-minimizer-webpack-plugin');

module.exports = {
  module: {
    rules: [
      {
        test: /\.css$/,
        use: [
          MiniCssExtractPlugin.loader, // 提取 CSS 为文件
          'css-loader' // 解析 CSS 依赖
        ]
      }
    ]
  },
  plugins: [
    new MiniCssExtractPlugin({
      filename: 'css/[name].[contenthash:8].css' // CSS 输出路径，用 contenthash 优化缓存
    })
  ],
  optimization: {
    minimizer: [
      '...', // 保留默认的 JS 压缩工具（TerserWebpackPlugin）
      new CssMinimizerPlugin() // 压缩 CSS
    ]
  }
};
```

**面试易错点**：`MiniCssExtractPlugin` 不能与 `style-loader` 同时使用（前者提取文件，后者注入 DOM，冲突）。

### 12.3 为什么 Tree-Shaking 不生效？如何排查？

**常见原因与排查步骤**：

1. **模块系统不是 ESM**：检查代码是否用 `require`（CommonJS），Babel 配置是否将 `modules` 设为 `commonjs`（需改为 `false`）。
2. **模式不是 production**：开发模式（`development`）下 `minimize: false`，未删除死代码，需切换到 `production` 模式。
3. **未配置 sideEffects**：`package.json` 未声明无副作用的文件，Webpack 为避免误删，不执行 Tree-Shaking，需添加 `sideEffects: ["*.css"]`。
4. **代码有副作用未声明**：如代码修改全局变量、引入 CSS，未在 `sideEffects` 中声明，导致 Webpack 不删除。

**排查工具**：开发模式下开启 `usedExports: true`，查看打包后的代码是否有 `/* unused harmony export xxx */` 注释（标记未使用的导出），若有则 Tree-Shaking 已标记，生产模式会删除。

### 12.4 如何优化 Webpack 构建速度？

**核心优化方向**：

1. 缩小构建范围：
   - `exclude: /node_modules/` 排除第三方库。
   - `include: path.resolve(__dirname, 'src')` 只处理 src 目录。
   - 用 `resolve.extensions` 限制后缀名查找顺序（如 `extensions: ['.js', '.jsx']`）。
   
2. 开启缓存：
   - `babel-loader` 配置 `cacheDirectory: true`。
   
     cacheDirectory: true // 开启缓存，加速二次构建（将编译结果缓存到 node_modules/.cache/babel-loader）
   
   - Webpack5 开启 `cache: { type: 'filesystem' }`（文件系统缓存）。
   
3. 并行构建：
   - `thread-loader` 多线程处理 JS/TS（如 `use: ['thread-loader', 'babel-loader']`）。
   - `TerserWebpackPlugin` 配置 `parallel: true`（多线程压缩 JS）。
   
4. 减少不必要的插件 / Loader：
   - 开发模式禁用 `MiniCssExtractPlugin`（用 `style-loader` 更快）。
   - 生产模式才启用压缩、清理插件。
   
5. 其他优化：
   - 用 `DllPlugin` 预打包第三方库（如 React、lodash），避免每次构建都编译。
   - 升级 Webpack 到最新版本（Webpack5 比 4 快 50%+）。

### 12.5 如何优化 Webpack 打包体积？

**核心优化方向**：

1. **Tree-Shaking**：开启 `usedExports: true` + `sideEffects`，删除死代码。
2. **代码分割**：用 `splitChunks` 提取第三方库和共享代码，避免重复打包。
3. 资源压缩：
   - JS：`TerserWebpackPlugin` 压缩（默认开启）。
   - CSS：`CssMinimizerPlugin` 压缩。
   - 图片：`image-webpack-loader` 压缩图片体积（如压缩 PNG/JPG）。
4. 按需加载：
   - 路由懒加载（如 React：`React.lazy(() => import('./Home'))`）。
   - 组件懒加载（如 `import('./Dialog').then(module => module.Dialog)`）。
5. 替换大依赖：
   - 用 `lodash-es` 替代 `lodash`（支持 Tree-Shaking，只引入用到的方法）。
   - 用 `dayjs` 替代 `moment.js`（体积小 80%+）。
6. **可视化分析**：用 `WebpackBundleAnalyzer` 分析包体积，定位大依赖（如 `node_modules` 中的大库）。

## 总结

本笔记覆盖 Webpack 与 Babel 的核心考点，从基础概念到进阶配置，再到面试高频问题，兼顾 “理解原理” 和 “实战配置”。面试时需重点掌握：

1. Webpack 构建流程、Compiler/Compilation 区别、Hash 类型、Loader/Plugin 区别。
2. Tree-Shaking、代码分割的原理与配置。
3. Babel 的三阶段流程、polyfill 配置、与 Webpack 的配合。
4. 优化构建速度和打包体积的实际方案。
