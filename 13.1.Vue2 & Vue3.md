### **生命周期钩子调整**

- **Vue2**：生命周期选项直接定义在组件选项中（如`created`、`mounted`）。
- **Vue3**：生命周期钩子需从vue中导入，在setup中调用，名称前加on：
  - `beforeCreate`、`created` → 被`setup`替代（`setup`执行时机等同于这两个钩子）；
  - `mounted` → `onMounted`；
  - `updated` → `onUpdated`；
  - `destroyed` → `onUnmounted`（命名更准确，体现 “卸载” 语义）。

### **组件写法与 API 风格**

- **Vue2**：采用`Options API`（选项式 API），将代码按`data`、`methods`、`computed`等选项分类。
  缺点：当组件逻辑复杂时，相关代码可能分散在不同选项中，维护成本高。
- **Vue3**：推荐`Composition API`（组合式 API），通过`setup`函数（或`<script setup>`语法糖）将相关逻辑聚合，更适合大型项目。
  优势：逻辑复用更灵活（替代 Vue2 的`mixin`，避免命名冲突），代码组织更清晰。

### **模板语法增强**

- 允许多根节点

  Vue3 重新设计了虚拟 DOM 的结构，引入了 “Fragment”（片段）类型的 VNode；

  当组件有多个根节点时，Vue3 会在内部创建一个**虚拟的 Fragment 节点**作为 “逻辑根”，但这个 Fragment 不会被渲染为实际的 DOM 元素

  ```vue
  <template>
  	<div>text 1</div>
  	<div>text 2</div>
  </template>
  ```

- 统一`v-model`与`.sync`修饰符语法

  - **Vue2** 中`v-model`与`.sync`修饰符并存（功能类似但写法不同）；

  ```vue
  <ChildComponent v-model="checked" :tite.sync="pageTitle" />
  <!-- 是以下的简写: -->
  <ChildComponent :check="checked" @change="checked = $event" :title="pageTitle" @update:title="pageTitle = $event" />
  
  <!-- ChildComponent.vue -->
  <script>
  export default {
    model: {
      prop: 'check',
      event: 'change'
    },
    props: {
      check: Boolean,
      title: String
    },
    methods: {
    	toggleCheck(bool) {
          this.$emit('change', bool);
      },
      updateTitle(newTitle) {
          this.$emit('update:title', newTitle);
      }
    }
  }
  </script>
  ```

  - **Vue3** 废除`.sync`，统一用`v-model:prop`语法，例如：

  ```vue
  <ChildComponent v-model="content" v-model:check="isChecked" v-model:title="pageTitle" />
  <!-- 是以下的简写: -->
  <ChildComponent
    :modelValue="content" @update:modelValue="content = $event"
    :check="isChecked" @update:check="isChecked = $event"
    :title="pageTitle" @update:title="pageTitle = $event" />
  
  <!-- ChildComponent.vue -->
  <script setup>
  const props = defineProps({
    modelValue: String, // 对应 v-model
    check: Boolean, // 对应 v-model:check
    title: String // 对应 v-model:title
  })
  const emit = defineEmits([
    'update:modelValue', // 对应 v-model 的更新
    'update:check',      // 对应 v-model:check 的更新
    'update:title'       // 对应 v-model:title 的更新
  ])
  // 示例：更新 modelValue（对应 v-model）
  const updateContent = (newVal) => {
    emit('update:modelValue', newVal)
  }
  // 示例：更新 check（对应 v-model:check）
  const toggleCheck = () => {
    emit('update:check', !props.check)
  }
  // 示例：更新 title（对应 v-model:title）
  const updateTitle = (newTitle) => {
    emit('update:title', newTitle)
  }
  </script>
  ```

- 其它增强

  - v-if 与 v-for 优先级跳转，**v-if** 优先级更高
  - 模板中可直接使用 **`?`** 可选链

### **全局API与实例创建**

- **Vue2**：全局 API 挂载在`Vue`构造函数上（如`Vue.use()`、`Vue.component()`），易造成全局污染。

- **Vue3**：通过`createApp`创建应用实例，全局 API 改为实例方法，避免全局污染：

  ```js
  // Vue2
  import Vue from 'vue'
  Vue.use(router)
  new Vue({ router }).$mount('#app')
  
  // Vue3
  import { createApp } from 'vue'
  const app = createApp(App)
  app.use(router)
  app.mount('#app')
  ```

### **新组件**

- **Teleport** 将组件内容渲染到指定 DOM 节点（如`body`），解决模态框嵌套层级过深导致的样式问题：

  ```vue
  <Teleport to="body / #app / .class / document.getElementById('custom-container')">
    <Modal />
  </Teleport>
  ```

- **Suspense** 等待异步组件加载完成，提供加载状态 fallback：

  ```vue
  <Suspense>
    <template #default>
      <AsyncComponent /> <!-- 异步组件 -->
    </template>
    <template #fallback>
      <Loading /> <!-- 加载中 -->
    </template>
  </Suspense>
  ```

### **静态树提升**

- 静态树提升

  <img src="https://i-coder.oss-cn-beijing.aliyuncs.com/files/20250826214503.png" style="zoom: 67%;" />

- 静态属性提升

  <img src="https://i-coder.oss-cn-beijing.aliyuncs.com/files/20250903160429.png" style="zoom:67%;" />

### **自定义指令**

**一、Vue2 与 Vue3 自定义指令的核心区别**

自定义指令用于封装 DOM 操作（如聚焦、权限控制、拖拽等），两者的差异主要体现在**钩子函数命名**、**参数结构**和**使用方式**上：

| 维度               | Vue2 自定义指令                                              | Vue3 自定义指令                                              |
| ------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| **钩子函数命名**   | 基于指令生命周期的特殊命名： `bind`（绑定）、`inserted`（插入 DOM）、`update`（更新）、`componentUpdated`（组件更新完成）、`unbind`（解绑） | 与组件生命周期钩子对齐（更统一）： `beforeMount`（绑定元素挂载前）、`mounted`（绑定元素挂载后）、`beforeUpdate`（更新前）、`updated`（更新后）、`beforeUnmount`（解绑前）、`unmounted`（解绑后） |
| **钩子参数传递**   | 钩子函数接收单独参数： `(el, binding, vnode, oldVnode)`      | 钩子函数接收一个包含所有参数的对象： `(el, binding, vnode, prevVnode)`（注意：`oldVnode` 改名为 `prevVnode`） |
| **指令注册方式**   | 全局注册：`Vue.directive('name', { ... })` 局部注册：组件内 `directives: { name: { ... } }` | 全局注册：`app.directive('name', { ... })` 局部注册：组件内 `directives: { name: { ... } }`（语法一致，但需配合 Vue3 的组件格式） |
| **响应式数据访问** | 需通过 `vnode.context` 访问组件实例数据（较繁琐）            | 可直接在钩子中访问组件的响应式数据（结合 `setup` 更灵活）    |
| **动态参数支持**   | 支持，但处理逻辑需在 `update` 钩子中手动实现                 | 原生支持，`beforeUpdate` 和 `updated` 钩子自动触发参数变化逻辑 |

**二、案例对比：实现「自动聚焦指令（v-focus）」**

以最常用的 “输入框自动聚焦” 为例，对比 Vue2 和 Vue3 的实现差异：

#### 1. Vue2 实现 v-focus

```javascript
// 全局注册
Vue.directive('focus', {
  // 当指令绑定的元素插入到DOM中时触发（适合执行DOM操作）
  inserted(el) {
    // el 是指令绑定的DOM元素
    el.focus(); // 调用原生聚焦方法
  }
});

// 局部注册（在组件内）
export default {
  directives: {
    focus: {
      inserted(el) {
        el.focus();
      }
    }
  }
};

// 使用：在模板中
<input v-focus type="text">
```

**钩子选择原因**：Vue2 中 `inserted` 钩子确保元素已插入 DOM，此时调用 `focus()` 才有效（`bind` 钩子阶段元素还未插入 DOM，调用无效）。

**2. Vue3 实现 v-focus**

```javascript
// 全局注册（在main.js中）
import { createApp } from 'vue';
const app = createApp(App);

app.directive('focus', {
  // 元素挂载到DOM后触发（对应Vue2的inserted）
  mounted(el) {
    el.focus(); // 同样调用原生聚焦方法
  }
});

// 局部注册（在组件内，支持setup语法糖）
export default {
  directives: {
    focus: {
      mounted(el) {
        el.focus();
      }
    }
  }
};

// 使用：在模板中（与Vue2一致）
<input v-focus type="text">
```

**钩子选择原因**：Vue3 的 `mounted` 钩子与组件的 `mounted` 一致，代表元素已挂载到 DOM，功能完全替代 Vue2 的 `inserted`。

**三、进阶案例：「权限控制指令（v-permission）」**

需求：根据用户权限决定元素是否显示（无权限则移除元素）。

#### 1. Vue2 实现 v-permission

```javascript
Vue.directive('permission', {
  // 绑定阶段获取权限列表（从组件实例中）
  bind(el, binding, vnode) {
    const { value } = binding; // 指令参数：v-permission="['admin']"
    const userPermissions = vnode.context.userPermissions; // 从组件实例取用户权限
    
    // 判断用户是否有权限
    if (!userPermissions.some(perm => value.includes(perm))) {
      // 无权限则移除元素（或隐藏）
      el.style.display = 'none';
      // 更彻底的移除：el.parentNode.removeChild(el)
    }
  },
  // 组件更新时重新检查权限
  update(el, binding, vnode, oldVnode) {
    // 逻辑同bind，确保权限变化时更新
    const { value } = binding;
    const userPermissions = vnode.context.userPermissions;
    if (!userPermissions.some(perm => value.includes(perm))) {
      el.style.display = 'none';
    } else {
      el.style.display = '';
    }
  }
});

// 使用：
<button v-permission="['admin']">删除（仅管理员可见）</button>
```

#### 2. Vue3 实现 v-permission

```javascript
app.directive('permission', {
  // 挂载时检查权限
  mounted(el, { value }, vnode) {
    const userPermissions = vnode.context.userPermissions; // 组件实例的权限数据
    if (!userPermissions.some(perm => value.includes(perm))) {
      el.style.display = 'none';
    }
  },
  // 更新时重新检查（对应Vue2的update）
  updated(el, { value }, vnode, prevVnode) {
    const userPermissions = vnode.context.userPermissions;
    if (!userPermissions.some(perm => value.includes(perm))) {
      el.style.display = 'none';
    } else {
      el.style.display = '';
    }
  }
});

// 使用（与Vue2一致）：
<button v-permission="['admin']">删除（仅管理员可见）</button>
```

**差异点**：Vue3 中钩子参数通过解构获取（`{ value }`），更简洁；且 `vnode.context` 仍可访问组件实例，但在组合式 API 中，需通过 `getCurrentInstance()` 暴露数据到实例上才能被指令访问。

### 四、面试高频问题与回答思路

1. **为什么 Vue3 要修改自定义指令的钩子命名？**
   答：Vue3 的钩子命名与组件生命周期保持一致（如 `mounted`、`updated`），降低了学习成本，使指令和组件的生命周期逻辑更统一，同时更直观地表达钩子的执行时机（如 `beforeMount` 明确是挂载前）。
2. **Vue2 的 `inserted` 和 Vue3 的 `mounted` 有什么区别？**
   答：功能上类似（都在元素插入 DOM 后执行），但 `inserted` 强调 “元素插入父节点”，而 `mounted` 强调 “元素完成挂载”，语义更贴合组件的生命周期，且 Vue3 中所有 DOM 相关操作都统一在 `mounted` 中处理，更清晰。
3. **自定义指令和组件的使用边界是什么？**
   答：- 自定义指令**专注于 DOM 操作**（如聚焦、样式控制、事件绑定）；
   - 组件**专注于 UI 复用和逻辑封装**（如按钮组件、表单组件）。
     当需要直接操作 DOM 元素时用指令，当需要复用完整 UI 结构和逻辑时用组件。
4. **如何在 Vue3 的自定义指令中使用响应式数据？**
   答：在组合式 API 中，可通过 `vnode.context` 访问组件的响应式数据（需确保数据暴露在组件实例上）；也可在指令钩子中使用 `watch` 或 `computed` 监听数据变化，实现动态更新（如权限变化时重新检查）。

### 总结

Vue2 和 Vue3 的自定义指令核心功能一致（封装 DOM 操作），但 Vue3 通过**钩子命名规范化**、**参数结构优化**使其更易用，且与组件生命周期的一致性更强。面试中需重点掌握钩子差异、使用场景及与组件的边界划分。

编辑分享