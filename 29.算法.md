### 实现一个 **长度为 100 的数组**，内容是 **1~100 的整数字符串**，要求 **不能重复**， 然后再进行 **排序**

| `compareFn(a, b)` 返回值 | 排序顺序                            |
| ------------------------ | ----------------------------------- |
| > 0                      | `a` 在 `b` 后，如 `[b, a]`， 降序   |
| < 0                      | `a` 在 `b` 前，如 `[a, b]`， 升序   |
| === 0                    | 保持 `a` 和 `b` 原来的顺序， 不排序 |

- #### Fisher–Yates 洗牌算法

```js
function createRandomArray() {
  // 先生成 1~100 的有序数组
  const arr = Array.from({ length: 100 }, (_, i) => (i + 1).toString());
  // Fisher–Yates 洗牌算法（打乱顺序，保证随机性均匀）
  for (let i = arr.length - 1; i > 0; i--) {
    const randomIndex = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[randomIndex]] = [arr[randomIndex], arr[i]];
  }
  // 排序：字符串比较默认按字典序，需要转成数字排序
  arr.sort((a, b) => Number(a) - Number(b));
  return arr;
}
console.log(createRandomArray());
```

**案例分析**：假设我们有一个数组 `[a,b,c,d]`（n=4）：

```js
for (let i = n - 1; i > 0; i--) {
  const j = Math.floor(Math.random() * (i + 1))
  [arr[i], arr[j]] = [arr[j], arr[i]]
}
```

- **第 1 轮**：`i = 3`，`j ∈ [0,3]`，4 种可能，概率 = **1/4**
  - 末尾元素 d 会被放到任意位置，概率都是 `1/4`
- **第 2 轮**：`i = 2`，`j ∈ [0,2]`，3 种可能，概率 = **1/3**
  - 现在只打乱前 3 个元素（a,b,c），每个元素都有 `1/3` 概率被选到第 2 轮交换位置。
- **第 3 轮**：`i = 1`，`j ∈ [0,1]`，2 种可能，概率 = **1/2**
  - 只打乱前 2 个元素，每个都有 `1/2` 概率被选到。

> - 是的，算法保证每轮都在 `[0, i]` 范围内均匀采样，每个索引出现的概率都是 `1/(i+1)`。
> - 通过数学归纳法可证明，最终每个元素出现在任意位置的概率都是 `1/n`，因此所有 `n!` 种排列概率相等。
> - 注意如果用 `sort(() => Math.random() - 0.5)` 就不是完全均匀，因为排序比较函数的调用次数和策略不固定，会导致概率偏差。

- #### Knuth 洗牌（正向实现）

```js
function createRandomArray() {
  const arr = Array.from({ length: 100 }, (_, i) => i + 1);
  for (let i = 0; i < arr.length; i++) {
    const randomIndex = Math.floor(Math.random() * (i + 1));
    [arr[i], arr[randomIndex]] = [arr[randomIndex], arr[i]];
  }
  return arr.sort((a, b) => a - b);
}
```

