## **1️⃣ 性能排序（从高到低）**

1. **Vue3 Template** ✅
2. Vue3 JSX
3. React JSX
4. Vue2 Template

**原因**：主要来自编译时优化、虚拟 DOM diff 算法、响应式系统效率三个核心因素。

## **2️⃣ 核心性能分析**

### **Vue3 Template 性能最优**

- **编译时优化**：
  - **静态节点提升**（Static Hoisting）：静态内容抽取为常量，跳过重复 diff。
  - **补丁标志 Patch Flags**：标记动态部分，只更新必要节点。
  - **树结构打平**：动态节点单独存储，减少遍历静态子树。
- **运行时优化**：
  - 虚拟 DOM diff 最小化，渲染函数高度优化。

### **Vue3 JSX **

- 没有模板编译时优化。
- 仍能受益于 Vue3 的 Proxy 响应式和高效 diff 算法。
- 性能略低于模板，但比 Vue2/React 高。

### **React JSX **

- JSX 编译为 `React.createElement`，生成完整虚拟 DOM。
- 性能依赖开发者手动优化（`React.memo`、`useMemo`）。
- Fiber 架构支持并发模式，提高用户体验，但基准性能仍低于 Vue3。

### **Vue2 Template**

- 静态节点缓存有限（只缓存静态根节点）。
- 无补丁标志，每次 diff 全量比对。

### Vue2 & Vue3 对比

### ⚙️ **一、响应式系统原理**

| **特性**         | **Vue 2**                          | **Vue 3**                        | **优势对比**                                                 |
| :--------------- | :--------------------------------- | :------------------------------- | :----------------------------------------------------------- |
| **实现机制**     | `Object.defineProperty` 劫持属性   | `Proxy` 代理整个对象             | Proxy 支持动态增删属性、数组索引修改等原生操作，无需 `Vue.set` |
| **数组监听**     | 重写数组方法（如 `push`/`splice`） | 直接监听数组索引和 `length` 变化 | 简化数组操作逻辑，避免 hack 处理                             |
| **深层响应**     | 初始化时递归遍历所有属性           | 惰性代理（访问嵌套属性时才代理） | 减少初始化开销，提升大型对象性能                             |
| **数据类型支持** | 仅支持 Object/Array                | 支持 Map/Set/WeakMap 等复杂类型  | 更适应现代 JS 数据结构                                       |

> 💡 **示例对比**
> **Vue 2 动态属性**：需 `this.$set(obj, 'newProp', value)`
> **Vue 3 动态属性**：直接 `obj.newProp = value`

### ⚡ **二、性能优化机制**

| **优化策略**     | **Vue 2**              | **Vue 3**                         | **性能提升效果**                   |
| :--------------- | :--------------------- | :-------------------------------- | :--------------------------------- |
| **编译优化**     | 无静态分析             | 静态提升（Hoisting） + PatchFlags | 减少 40% 虚拟 DOM 比对开销         |
| **Tree-shaking** | 不支持（全量引入）     | 基于 ESM 的按需编译               | 打包体积减少 30%（gzip 后约 10KB） |
| **虚拟 DOM**     | 全量 Diff（同层比较）  | 动态节点标记 + 区块树优化         | 渲染速度提升 1.5-2 倍              |
| **内存占用**     | 较高（递归响应式绑定） | 更低（惰性代理 + 缓存机制）       | 大型应用内存减少 50%               |

#### 关键优化技术：

- **静态提升**：将静态节点转为常量复用，避免重复创建
- **事件监听缓存**：避免重复生成事件函数
- **SSR 优化**：静态内容直出为 HTML 字符串，跳过虚拟 DOM



# Vue2/Vue3/React18 综合对比表格

### 一、响应式原理：状态变化如何触发视图更新

响应式原理决定了 “状态变化后如何被感知并触发更新”

| 框架    | 核心实现                                           | 关键特点                                                     | 局限性 / 优化点                                              |
| ------- | -------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | `Object.defineProperty` 拦截单个属性的 `get`/`set` | 1. 初始化时递归遍历 `data`，为每个属性绑定 `get`（收集依赖）和 `set`（触发更新）； 2. 依赖通过 `Watcher` 管理，每个组件对应一个 `Watcher`； 3. 数组通过重写 `push/pop` 等 7 个方法实现响应式。 | 1. 无法监听 **对象新增 / 删除属性**（需 `$set`/`Vue.set`）； 2. 无法监听 **数组索引 / 长度变化**（需 `splice` 等重写方法）； 3. 递归初始化耗时（大数据对象性能差）。 |
| Vue3    | `Proxy` 代理整个对象 + `Reflect` 反射              | 1. 直接代理整个对象（非单个属性），天然支持 **对象新增 / 删除属性**、**数组索引变化**； 2. 懒代理：仅访问子对象时才代理，避免递归初始化； 3. 编译时结合 `Track`（标记依赖）和 `Trigger`（触发更新），更新粒度更细（只触发依赖组件）； 4. 支持 `Ref`（基础类型响应式）、`Reactive`（对象响应式）。 | 1. 不兼容 IE 浏览器（`Proxy` 无 polyfill）； 2. 基础类型需用 `Ref` 包裹（否则无法响应）。 |
| React18 | 非自动响应式，基于 “状态更新触发重渲染” 机制       | 1. 无依赖收集，需通过 `setState`（类组件）或 `useState`/`useReducer`（函数组件）**手动触发更新**； 2. 更新后默认触发 **组件及其所有子组件重渲染**（需手动优化）； 3. 结合 **并发渲染**（Concurrent Rendering），支持高优先级任务（如输入）中断低优先级更新。 | 1. 冗余重渲染（需 `memo`/`useMemo`/`useCallback` 优化）； 2. 状态更新是异步批处理（`setState` 后立即读状态为旧值）。 |

**核心区别**：

- Vue2/Vue3 是 “自动响应式”（自动收集依赖、触发更新），Vue3 因 `Proxy` 解决了 Vue2 的局限性；
- React18 是 “手动触发更新”（需显式调用更新函数），依赖运行时调度控制更新优先级。

### 二、diff 算法：对比虚拟 DOM 差异的策略

diff 算法的核心是 “高效找出新旧虚拟 DOM 的差异”

| 框架    | 核心实现                                                     | 关键特点                                                     | 性能优化点                                                   |
| ------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 双端指针同层对比（`updateChildren` 函数）                    | 1. 仅对比 **同一层级节点**（不跨层），基于 `key + tag`（节点类型）判断可复用； 2. 4 个指针（旧头 / 旧尾、新头 / 新尾）从两端向中间对比，处理新增 / 删除 / 移动； 3. 同步执行，一旦开始必须完成（无法中断）。 | 1. `key` 唯一标识节点（避免用 index 导致复用错误）； 2. 同层对比将时间复杂度降为 O (n)（n 为节点数）。 |
| Vue3    | 静态标记（Static Hoisting）+ 补丁标志（PatchFlags）+ 最长递增子序列（LIS） | 1. 编译时标记 **静态节点**（如纯文本，不参与 Diff）和 **动态节点的变化范围**（如仅文本变化 `PatchFlags.TEXT`）； 2. 运行时仅对比带 `PatchFlags` 的动态节点，跳过静态内容； 3. 多节点移动用 LIS 计算最少移动次数（比 Vue2 双指针更高效）。 | 1. 静态节点复用（无需每次对比）； 2. 动态节点精准对比（只处理变化部分），对比量大幅减少。 |
| React18 | Fiber 架构 + 车道模型（Lane Model）+ 可中断协调              | 1. 将虚拟 DOM 转为 **Fiber 链表**（每个节点含 `child/sibling/return` 指针），支持暂停 / 恢复； 2. 分阶段对比： - 同向遍历 → 反向遍历 → 剩余节点处理（类似 Vue2，但基于 Fiber 链表）； - 用 `key + type` 判断可复用，标记 `EffectTag`（如插入 / 删除 / 更新）； 3. 车道模型（Lane）分配优先级，高优任务（如点击）可中断低优 Diff。 | 1. 可中断 Diff（避免主线程阻塞）； 2. 优先级调度（优先响应用户交互）； 3. Fiber 链表减少递归栈开销（避免栈溢出）。 |

**核心区别**：

- Vue2/Vue3 侧重 “减少对比量”（Vue3 通过编译时优化做到极致）；
- React18 侧重 “灵活调度对比时机”（通过可中断机制避免主线程阻塞）。

### 三、patch 算法：将差异应用到真实 DOM 的过程

patch 算法是 “将 diff 找到的差异同步到真实 DOM” 的过程

| 框架    | 核心实现                                               | 关键特点                                                     | 执行时机与安全性                                             |
| ------- | ------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 边 Diff 边 Patch（同步执行）                           | 1. 先通过 `sameVnode` 判断节点是否可复用： - 不可复用：销毁旧 DOM → 创建新 DOM → 插入； - 可复用：进入 `patchVnode`，更新属性 / 文本 → 递归处理子节点； 2. DOM 操作与 Diff 交织，同步生效。 | 1. 同步执行，DOM 操作立即完成（无 UI 不一致风险）； 2. 无批量 DOM 操作（频繁更新可能引发重排）。 |
| Vue3    | 基于 PatchFlags 的精准 Patch（动态节点 边Diff边Patch） | 1. 利用编译时标记的 `PatchFlags`，直接定位动态节点的操作类型： - `TEXT`：仅更新文本； - `CLASS`：仅更新类名； - `PROPS`：仅更新指定属性； 2. 静态节点直接复用，无需 DOM 操作。 | 1. 同步执行，但 DOM 操作量极少（只处理动态部分）； 2. 批量更新（同一事件循环内的多次状态变化合并为一次 Patch）。 |
| React18 | 分阶段执行：协调阶段（Diff）→ 提交阶段（Patch）        | 1. 协调阶段（可中断）：仅标记 `EffectTag`（如 `Placement`/`Update`/`Deletion`），不操作 DOM； 2. 提交阶段（不可中断）： - `Before Mutation`：执行 `getSnapshotBeforeUpdate` 等前置逻辑； - `Mutation`：批量执行 DOM 操作（插入 / 删除 / 更新）； - `Layout`：执行 `componentDidUpdate`/`useLayoutEffect` 等后置逻辑。 | 1. 提交阶段不可中断（避免 DOM 操作半截导致 UI 错乱）； 2. 批量 DOM 操作（减少重排重绘）； 3. `useEffect` 在 DOM 操作后异步执行（不阻塞渲染）。 |

**核心区别**：

- Vue2/Vue3 是 “边 diff 边 patch”（同步执行），Vue3 因编译时优化，patch 操作更精准；
- React18 是 “先 diff 标记差异，再集中 patch”（分阶段），且 diff 阶段可中断，patch 阶段不可中断。

### 四、组件通信方式：组件间数据传递的手段

组件通信是 “模块化开发” 的核心，不同框架的主流方案差异较大。

| 框架    | 常用通信方式                                                 | 适用场景                                                     | 优缺点对比                                                   |
| ------- | ------------------------------------------------------------ | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 1. `props`/`$emit`：父子组件通信； 2. `$parent`/`$children`：直接访问父子实例； 3. `provide`/`inject`：跨层级通信； 4. EventBus（`Vue.prototype.$bus = new Vue()`）：任意组件通信； 5. Vuex：全局状态管理。 | 1. 父子：`props`/`$emit`； 2. 跨层级：`provide`/`inject`； 3. 全局：Vuex/EventBus。 | 优点：API 简洁，父子通信直观； 缺点：`$parent`/`$children` 耦合度高，EventBus 无类型校验易出错。 |
| Vue3    | 1. `props`/`emit`：父子通信（`emit` 需在 `defineEmits` 中声明）； 2. `provide`/`inject`：跨层级通信（支持类型推导）； 3. mitt.js：替代 EventBus（轻量，支持取消订阅）； 4. Pinia：全局状态管理（官方推荐，支持 Vue3 组合式 API）； 5. `expose`/`ref`：父组件访问子组件暴露的方法 / 属性。 | 1. 父子：`props`/`emit`/`expose`； 2. 跨层级：`provide`/`inject`； 3. 全局：Pinia。 | 优点：类型安全（TS 友好），Pinia 比 Vuex 轻量； 缺点：`provide`/`inject` 仍无强制类型约束（需手动定义）。 |
| React18 | 1. `props`：父子通信（单向数据流，子组件不可修改 props）； 2. `Context`：跨层级通信（避免 props 透传）； 3. `useImperativeHandle`/`ref`：父组件访问子组件方法 / 属性； 4. Redux Toolkit：全局状态管理（主流，支持中间件）； 5. Zustand/Jotai：轻量全局状态（替代 Redux，API 更简洁）。 | 1. 父子：`props`/`useImperativeHandle`； 2. 跨层级：`Context`； 3. 全局：Redux Toolkit/Zustand。 | 优点：单向数据流清晰（易调试）， Zustand 比 Redux 轻量； 缺点：`Context` 频繁更新会导致所有消费者重渲染（需配合 `memo` 优化）。 |

**核心区别**：
Vue 提供更多 “原生跨层级通信 API”（`provide`/`inject`），React 更依赖 “第三方状态库” 或 `Context`（需手动优化性能）。

### 五、生命周期 / 钩子机制：组件生命周期的控制

生命周期是 “组件创建→更新→销毁” 的钩子，决定业务逻辑的执行时机。

| 框架    | 核心机制                                          | 常用钩子 / API                                               | 关键差异                                                     |
| ------- | ------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 选项式生命周期（Options API）                     | 1. 创建：`beforeCreate` → `created`（无 DOM）； 2. 挂载：`beforeMount` → `mounted`（有 DOM）； 3. 更新：`beforeUpdate` → `updated`； 4. 销毁：`beforeDestroy` → `destroyed`。 | 1. 钩子与组件实例绑定，`this` 指向组件实例； 2. 销毁阶段需手动清理副作用（如事件监听）。 |
| Vue3    | 组合式 API 生命周期（`onXxx` 函数）+ 选项式兼容   | 1. 创建：无（`setup` 替代 `beforeCreate`/`created`）； 2. 挂载：`onBeforeMount` → `onMounted`； 3. 更新：`onBeforeUpdate` → `onUpdated`； 4. 卸载：`onBeforeUnmount` → `onUnmounted`； 5. 新增：`onErrorCaptured`（捕获子组件错误）。 | 1. 组合式 API 中钩子需手动导入，逻辑可拆分（更灵活）； 2. `setup` 中无 `this`（需用 `ref`/`reactive` 管理状态）。 |
| React18 | 类组件生命周期 + 函数组件 Hooks（`useEffect` 等） | 1. 类组件： - 挂载：`constructor` → `componentDidMount`； - 更新：`shouldComponentUpdate` → `componentDidUpdate`； - 卸载：`componentWillUnmount`； 2. 函数组件（主流）： - `useEffect`：替代 `componentDidMount`/`componentDidUpdate`/`componentWillUnmount`； - `useLayoutEffect`：DOM 操作后同步执行（类似 `componentDidUpdate`）。 | 1. 函数组件用 Hooks 替代生命周期（逻辑复用更优）； 2. `useEffect` 异步执行（避免阻塞渲染），`useLayoutEffect` 同步执行（适合依赖 DOM 布局的逻辑）。 |

**核心区别**：
Vue3 用 “组合式 API 钩子” 拆分逻辑，React 用 “Hooks” 替代类组件生命周期（更适合函数式编程）。

### 六、状态管理方案：全局 / 跨组件状态的管理

状态管理是 “复杂应用” 的核心，决定状态的可维护性与性能。

| 框架    | 官方推荐方案                                         | 主流第三方方案                                               | 核心特点                                                     |
| ------- | ---------------------------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | Vuex（官方状态管理库）                               | -                                                            | 1. 核心概念：`State`（状态）→ `Getter`（计算属性）→ `Mutation`（同步修改）→ `Action`（异步修改）； 2. 单一状态树（Single Source of Truth）； 3. 支持模块化（`modules`），解决状态冗余。 |
| Vue3    | Pinia（官方推荐，替代 Vuex）                         | -                                                            | 1. 兼容 Vue3 组合式 API，支持 `setup` 语法； 2. 无 `Mutation`（直接用 `action` 处理同步 / 异步）； 3. 支持多状态树（无需 `modules`），TS 类型推导更友好； 4. 轻量（比 Vuex 体积小）。 |
| React18 | 无官方推荐（原生 `Context` + `useReducer` 基础方案） | 1. Redux Toolkit（RTK，Redux 官方简化版）； 2. Zustand（轻量，API 简洁）； 3. Jotai/Recoil（原子化状态管理）。 | 1. `Context` + `useReducer`：适合中小型应用（无需第三方库）； 2. RTK：适合大型应用（支持中间件、持久化）； 3. Zustand：比 RTK 轻量，无需 `Provider` 包裹； 4. 原子化方案（Jotai）：只更新依赖的状态（性能更优）。 |

**核心区别**：
Vue 有 “官方指定状态库”（Vuex→Pinia），React 更依赖 “社区方案”（灵活但需选型）。

### 七、MVVM/MVC 架构归属：框架的设计范式

架构范式决定 “数据、视图、逻辑” 的分离方式。

| 框架      | 架构归属                        | 核心逻辑                                                     | 与架构的适配性                                               |
| --------- | ------------------------------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2/Vue3 | MVVM（Model-View-ViewModel）    | 1. Model：`data`/`reactive` 中的数据； 2. View：模板（HTML + 指令）； 3. ViewModel：组件实例（Vue 内部实现，负责数据绑定：Model 变 → View 自动更，View 交互 → Model 自动更）。 | 1. 完全符合 MVVM：ViewModel 隔离 View 与 Model，无需手动操作 DOM； 2. Vue3 组合式 API 进一步强化 ViewModel 的逻辑拆分能力。 |
| React18   | 声明式 UI 库（非严格 MVC/MVVM） | 1. 更偏向 “View 层”：核心是 “组件渲染”，状态管理需额外配合（如 Redux/Context）； 2. 设计范式：`UI = f(State)`（UI 是状态的函数，状态变 → UI 重新渲染）。 | 1. 不严格属于 MVC/MVVM：无内置 ViewModel 层，需手动关联 State 与 UI； 2. 可通过 “状态管理库 + 组件” 实现类似 MVC 的分层（如 Redux 作为 Model，组件作为 View/Controller）。 |

### MVVM & MVC

- **MVC（Model-View-Controller）**

  在 MVC 架构中，View 和 Controller 之间存在较强的依赖关系。用户输入由 Controller 处理，Controller 更新 Model，然后 Model 通知 View 更新。这样的双向通信使得 View 和 Controller 的耦合度较高。数据变化需要手动更新视图

  - Model：数据（代表应用程序的数据模型和业务逻辑，负责处理数据存储和检索）
  - View：视图（用户界面，展示数据给用户，并接收用户输入）
  - Controller：控制逻辑（处理用户输入，将用户操作转换为Model更新，并通知View更新）

- **MVVM（Model-View-ViewModel）**

  当 Model 数据发生变化时，View 会自动更新；当用户在 View 中进行操作时，ViewModel 会自动更新 Model

  - Model：数据（应用程序的数据模型和业务逻辑）
  - View：视图（代表用户界面，负责展示数据和接收用户输入；View不直接与Model交互）
  - ViewModel：数据绑定逻辑（View和Model的中介，负责将Model中的数据转换View可以显示的数据，同时将View的用户输入转换为Model可以理解的操作）