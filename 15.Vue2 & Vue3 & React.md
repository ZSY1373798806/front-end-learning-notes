## **1️⃣ 性能排序（从高到低）**

1. **Vue3 Template** ✅
2. Vue3 JSX
3. React JSX
4. Vue2 Template

**原因**：主要来自编译时优化、虚拟 DOM diff 算法、响应式系统效率三个核心因素。

## **2️⃣ 核心性能分析**

### **Vue3 Template 性能最优**

- **编译时优化**：
  - **静态节点提升**（Static Hoisting）：静态内容抽取为常量，跳过重复 diff。
  - **补丁标志 Patch Flags**：标记动态部分，只更新必要节点。
  - **树结构打平**：动态节点单独存储，减少遍历静态子树。
- **运行时优化**：
  - 虚拟 DOM diff 最小化，渲染函数高度优化。

### **Vue3 JSX **

- 没有模板编译时优化。
- 仍能受益于 Vue3 的 Proxy 响应式和高效 diff 算法。
- 性能略低于模板，但比 Vue2/React 高。

### **React JSX **

- JSX 编译为 `React.createElement`，生成完整虚拟 DOM。
- 性能依赖开发者手动优化（`React.memo`、`useMemo`）。
- Fiber 架构支持并发模式，提高用户体验，但基准性能仍低于 Vue3。

### **Vue2 Template**

- 静态节点缓存有限（只缓存静态根节点）。
- 无补丁标志，每次 diff 全量比对。

### Vue2 & Vue3 对比

### ⚙️ **一、响应式系统原理**

| **特性**         | **Vue 2**                          | **Vue 3**                        | **优势对比**                                                 |
| :--------------- | :--------------------------------- | :------------------------------- | :----------------------------------------------------------- |
| **实现机制**     | `Object.defineProperty` 劫持属性   | `Proxy` 代理整个对象             | Proxy 支持动态增删属性、数组索引修改等原生操作，无需 `Vue.set` |
| **数组监听**     | 重写数组方法（如 `push`/`splice`） | 直接监听数组索引和 `length` 变化 | 简化数组操作逻辑，避免 hack 处理                             |
| **深层响应**     | 初始化时递归遍历所有属性           | 惰性代理（访问嵌套属性时才代理） | 减少初始化开销，提升大型对象性能                             |
| **数据类型支持** | 仅支持 Object/Array                | 支持 Map/Set/WeakMap 等复杂类型  | 更适应现代 JS 数据结构                                       |

> 💡 **示例对比**
> **Vue 2 动态属性**：需 `this.$set(obj, 'newProp', value)`
> **Vue 3 动态属性**：直接 `obj.newProp = value`

### ⚡ **二、性能优化机制**

| **优化策略**     | **Vue 2**              | **Vue 3**                         | **性能提升效果**                   |
| :--------------- | :--------------------- | :-------------------------------- | :--------------------------------- |
| **编译优化**     | 无静态分析             | 静态提升（Hoisting） + PatchFlags | 减少 40% 虚拟 DOM 比对开销         |
| **Tree-shaking** | 不支持（全量引入）     | 基于 ESM 的按需编译               | 打包体积减少 30%（gzip 后约 10KB） |
| **虚拟 DOM**     | 全量 Diff（同层比较）  | 动态节点标记 + 区块树优化         | 渲染速度提升 1.5-2 倍              |
| **内存占用**     | 较高（递归响应式绑定） | 更低（惰性代理 + 缓存机制）       | 大型应用内存减少 50%               |

#### 关键优化技术：

- **静态提升**：将静态节点转为常量复用，避免重复创建
- **事件监听缓存**：避免重复生成事件函数
- **SSR 优化**：静态内容直出为 HTML 字符串，跳过虚拟 DOM



# Vue2/Vue3/React18 综合对比表格

### 一、响应式原理：状态变化如何触发视图更新

响应式原理决定了 “状态变化后如何被感知并触发更新”

| 框架    | 核心实现                                         | 关键特点                                                     |
| ------- | ------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 基于 `Object.defineProperty` 拦截属性读写        | 1. 初始化时递归遍历数据，对每个属性的 `get`/`set` 进行拦截； 2. `get` 时收集依赖（Watcher），`set` 时通知依赖更新； 3. 局限性：无法监听对象新增 / 删除属性、数组索引 / 长度变化（需通过 `$set` 等 API 手动触发）。 |
| Vue3    | 基于 `Proxy` 代理整个对象                        | 1. 直接代理整个对象（而非单个属性），天然支持监听对象新增 / 删除属性、数组索引变化； 2. 配合 `Reflect` 实现更完整的拦截（如 `in` 操作符、`delete` 等）； 3. 编译时与响应式结合（如 Track 标记依赖），更新粒度更细（只触发依赖该状态的节点）。 |
| React18 | 非响应式系统，基于 “状态更新触发重新渲染” 的机制 | 1. 无自动依赖收集，状态变化需通过 `setState` 或 `useState` 的更新函数手动触发； 2. 状态更新后，默认触发组件及其子组件的重新渲染（需通过 `memo`/`useMemo` 优化）； 3. 结合并发渲染，可通过 `useTransition` 标记低优先级更新，避免阻塞。 |

**核心区别**：

- Vue2/Vue3 是 “自动响应式”（自动收集依赖、触发更新），Vue3 因 `Proxy` 解决了 Vue2 的局限性；
- React18 是 “手动触发更新”（需显式调用更新函数），依赖运行时调度控制更新优先级。

### 二、diff 算法：对比虚拟 DOM 差异的策略

diff 算法的核心是 “高效找出新旧虚拟 DOM 的差异”

| 框架    | 核心实现                                                     | 关键特点                                                     |
| ------- | ------------------------------------------------------------ | ------------------------------------------------------------ |
| Vue2    | 双指针同层对比（`updateChildren` 函数）                      | 1. 只对比同一层级节点； 2. 依赖 `key` 识别可复用节点； 3. 同步执行，一旦开始必须完成，不可中断；4. 无静态标记，全量遍历 |
| Vue3    | 基于静态标记（Static Hoisting）和补丁标志（PatchFlags）的精准对比 | 1. 编译时标记静态节点（不参与 diff）和动态节点的变化范围（如仅文本 / 类名变化）； 2. 运行时只对比带标记的动态节点，跳过静态内容； 3. 仍同步执行，但对比范围大幅缩小，效率更高。 |
| React18 | 基于 Fiber 架构的可中断 diff（协调阶段 Reconciliation）      | 1. 将虚拟 DOM 转换为 Fiber 链表（支持暂停 / 恢复）； 2. 采用 “深度优先 + 优先级调度”，高优先级任务（如用户输入）可中断低优先级 diff； 3. 对比时结合 `key` 和 type 判断，支持节点移动的高效处理。 |

**核心区别**：

- Vue2/Vue3 侧重 “减少对比量”（Vue3 通过编译时优化做到极致）；
- React18 侧重 “灵活调度对比时机”（通过可中断机制避免主线程阻塞）。

### 三、patch 算法：将差异应用到真实 DOM 的过程

patch 算法是 “将 diff 找到的差异同步到真实 DOM” 的过程

| 框架    | 核心实现                                             | 关键特点                                                     |
| ------- | ---------------------------------------------------- | ------------------------------------------------------------ |
| Vue2    | 同步执行，diff 与 patch 交织进行                     | 1. 先通过 `sameVnode` 判断节点是否可复用，不可复用则直接销毁旧节点、创建新节点； 2. 可复用则进入 `patchVnode`，更新属性 / 文本，并调用 `updateChildren` 处理子节点； 3. 整个过程同步执行，DOM 操作立即生效。 |
| Vue3    | 基于编译时信息的精准 patch                           | 1. 利用 PatchFlags 直接定位动态节点的变化类型（如仅更新文本 / 类名）； 2. 静态节点直接复用，无需操作 DOM； 3. 同步执行，但 DOM 操作量极少（只处理动态部分）。 |
| React18 | 分阶段执行：协调阶段（可中断）+ 提交阶段（不可中断） | 1. 协调阶段（diff）：找出差异并标记需要执行的 DOM 操作（如插入 / 删除 / 更新），可被高优先级任务中断； 2. 提交阶段（patch）：一次性执行所有 DOM 操作，不可中断（避免 UI 不一致）； 3. 依赖 Scheduler 调度，确保高优先级更新优先 patch。 |

**核心区别**：

- Vue2/Vue3 是 “边 diff 边 patch”（同步执行），Vue3 因编译时优化，patch 操作更精准；
- React18 是 “先 diff 标记差异，再集中 patch”（分阶段），且 diff 阶段可中断，patch 阶段不可中断。

### MVVM & MVC

- **MVC（Model-View-Controller）**

  在 MVC 架构中，View 和 Controller 之间存在较强的依赖关系。用户输入由 Controller 处理，Controller 更新 Model，然后 Model 通知 View 更新。这样的双向通信使得 View 和 Controller 的耦合度较高。数据变化需要手动更新视图

  - Model：数据（代表应用程序的数据模型和业务逻辑，负责处理数据存储和检索）
  - View：视图（用户界面，展示数据给用户，并接收用户输入）
  - Controller：控制逻辑（处理用户输入，将用户操作转换为Model更新，并通知View更新）

- **MVVM（Model-View-ViewModel）**

  当 Model 数据发生变化时，View 会自动更新；当用户在 View 中进行操作时，ViewModel 会自动更新 Model

  - Model：数据（应用程序的数据模型和业务逻辑）
  - View：视图（代表用户界面，负责展示数据和接收用户输入；View不直接与Model交互）
  - ViewModel：数据绑定逻辑（View和Model的中介，负责将Model中的数据转换View可以显示的数据，同时将View的用户输入转换为Model可以理解的操作）